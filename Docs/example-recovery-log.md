# Example recovery log

This is the short causal ledger for the opt-in end-to-end examples. Each entry
records the observed failure, the controlled change, and the result so that a
later investigation does not repeat an already-disproven retry.

## Massless pentagon

| ID | Observed issue | Controlled change | Result |
|---|---|---|---|
| P-01 | One global matching-digit setting made a lower consumer stricter while leaving its producer unchanged. | Bind matching digits per producing level and bind checkpoints to the producer's value. | Producer retries now tighten only the preceding level. |
| P-02 | Tightening one producer exposed the same uncertainty at the next boundary. | Enforce a transitive two-digit safety ladder through all earlier producers. | Levels 2, 3, and 4 stabilize at 12, 14, and 16 digits for the eight-digit public profile. |
| P-03 | A singular rational chart had a passing midpoint and uncertainty only in its solved weights. | Retry locally with the retained exact Rational shadow and certify the physical residual. | The level-2 upper singular match passes without another upstream retry. |
| P-04 | Level-1 match 12, a regular receiver, had basis=pass, incoming=pass, weights=inconclusive. | Re-solve once with zero-radius midpoint data as a proposal, retaining the untouched full-ball forward residual as publication authority. | Match 12 passes. A negative unit test proves a midpoint candidate is rejected when the full-ball residual remains inconclusive. |
| P-05 | After P-04, level-1 match 24 honestly reached only epsilon power 2 while power 3 was required. | Increase only the private level-1 matching halo from 2 to 3. | Fresh transport completed in 92.67 seconds with finite value `-0.0254117799`; no discovery retry was used. |

Current pinned production profile: working precision 300, Taylor order 25,
matching digits 8, private halos `<|1 -> 3, 2 -> 1|>`, and producer digits
`<|2 -> 12, 3 -> 14, 4 -> 16|>`.

## Planar double box

| ID | Observed issue | Controlled change | Result |
|---|---|---|---|
| D-01 | A level-6 terminal match requested an incoming Taylor prefix through order 25 although the incoming source honestly retained only order 0. The basis source retained order 25. | Give each retained source its own Taylor-prefix ownership. Basis materialization uses the basis width; continuity replay uses the incoming width. | Level 6 passes this match. A persistent-hop regression fixture has basis width 3 and incoming width 1; the old shared-width behavior aborts that fixture. |
| D-02 | The Taylor-prefix exception reported only the requested order, hiding which source had been over-read. | Report the requested order, retained complete maximum, source checkpoint, and requesting checkpoint. | The next occurrence identified the asymmetric incoming source immediately. A unit test pins the diagnostic fields. |
| D-03 | The timing script called `runExample` directly, so a retryable native failure terminated the run instead of using the production retry driver. | Route the timing gate through `ft2RunExampleWithMatchingRetries` and allow pinned initial halo/Taylor/digit profiles. | The gate now performs the same bounded retries as production. A Wolfram regression test pins preservation of seeded profiles across a retry. |
| D-04 | At level 5, materialize-then-evaluate continuity produced a wider reassociated ball. Its midpoint difference was about `2e-16`; the two balls overlapped, but the residual upper bound narrowly exceeded the standalone relative threshold. Raising Taylor order from 25 to 50 did not improve it. | Accept a materialized continuity replay when the receiving and incoming balls overlap; retain the ordinary tolerance check for disjoint balls. The original retained match remains the accuracy authority. | Level 5 completes. Unit tests cover an overlapping pair that exceeds the residual threshold and a disjoint pair that remains rejected. |
| D-05 | A terminal endpoint ball slightly wider than the publication threshold returned an unstructured generic error, preventing a targeted retry. | Add the typed `TerminalOutputInconclusive` error and translate it into a level-accuracy retry request. | The retry driver identifies the producing level and retries instead of returning `$Failed`. C++ and Wolfram focused suites cover the typed path and translation. |
| D-06 | The D-05 retry raised level 5 from 10 to 12 digits and recursively raised deepest level 6 to 14. Level 6 then failed its own last match at 14 digits with a passing midpoint and propagated enclosure, and there is no higher producer to retry. | Separate the contracts: retain the producing level's guarded internal match tolerance, but bind every endpoint request/checkpoint to the explicit accuracy required by its level-1 consumer. Do not raise Taylor order or working precision for this symptom; level 6 already completed at 12 digits. | The new unit case rejects one ball at producer `1e-10` yet accepts that unchanged ball at consumer `1e-8`; malformed and checkpoint-round-trip endpoint tests cover the new contract. All 48 C++ tests, endpoint bridge 6/6, and runner tests 45/45 pass. A clean gate still eventually escalated level 6 to 14, proving endpoint publication was only one trigger and not the complete cause. |
| D-07 | The first post-D-06 gate reached levels 6, 5, and 4 quickly, but a later propagated-enclosure retry again constructed `<|5 -> 12, 6 -> 14|>` and failed deepest level-6 match 16. The gate deleted its scratch log on failure, hiding the immediately preceding retry in truncated console output. | Preserve the scratch directory automatically on a failed timing gate (and optionally on success) so the full retry sequence remains auditable. | The preserved 64-line reproducer shows no terminal-output failure. Level 4 at its unchanged 8-digit target fails once with level 5 at 8 digits and again with level 5 at 10, then legitimately requests level 5 at 12. The generic transitive guard—not endpoint publication—automatically raises level 6 from 12 to 14. |
| D-08 | It is unknown whether a level-5 solve at 12 digits actually needs a 14-digit level-6 boundary. The generic ladder assumes every producer consumes another two guarded digits, but level 5 already completed with a level-6 boundary at 12. | Controlled profile test only: seed `<|5 -> 12, 6 -> 12|>`, holding Taylor order, working precision, halos, and all public targets fixed. | Rejected. Level 5 itself fails at its first singular-balanced handoff with target `1e-12`, so equal upstream accuracy is insufficient; the driver then correctly asks for level 6 at 14. |
| D-09 | Level-6 target 14 fails only at match 16 after the incoming enclosure has traversed the full deepest chart chain. Its midpoint and basis probes pass; incoming and weights are inconclusive. This differs from D-04, where raising level-5 Taylor order did not change a one-step materialization replay. | Controlled profile test only: seed digits `<|5 -> 12, 6 -> 14|>` and raise Taylor order only at level 6 from 25 to 50. | Confirmed. Level 6 completes at 14 and publishes a much tighter boundary. Working precision is not the limiter. Level 5 then advances from its first lower match to upper match 8 before encountering a new failure. |
| D-10 | Level-5 upper match 8 at target `1e-12` has exactly two inconclusive coefficients (epsilon 14 and 15). The midpoint residual passes, and the basis-only, weights-only, and incoming-only full-ball probes all pass; only the recombined full residual is inconclusive. The regular receiving chart has no singular normal-frame route. | Diagnosis first: inspect the residual-source classifier and whether the reconstructed and incoming coefficient balls overlap. Do not request more producer digits when every isolated source probe already passes. | Resolved by D-11. The match was overconstrained by conflating the level's computation accuracy with the lower consumer's hop-certification contract; after splitting those contracts, level 5 including this match completes. |
| D-11 | D-10 shows that raising a level's computation target also raises every internal hop's certification target, even when that level only has to publish fewer digits to its downstream consumer. For level 5, computation at 12 digits is needed to tighten the level-4 boundary, but requiring all level-5 hops to certify at 12 digits overconstrains a boundary that is consumed at 8 digits. | Split internal matching certification from computation/chop accuracy and endpoint publication. Keep computation at the level target, keep endpoint publication explicit, and set hop certification to `Min[level target, downstream publication digits + safety digits]`. This gives level 5 computation/certification/publication targets `12/10/8`, while deepest level 6 remains `14/14/12`. | Confirmed. Focused suites pass (runner 46/46, native observable batch 27/27, endpoint bridge 6/6). The controlled run completes levels 6 and 5, including the former D-10 match, and reaches level 4 in 21 seconds. |
| D-12 | Level-4 upper match 7 at its ordinary `1e-8` target has four narrowly inconclusive coefficients: their residual upper bounds are only about 1.02--1.26 times the allowed bounds. The zero-radius midpoint and the basis-only, weights-only, and incoming-only full-ball probes all pass. The current producer classifier returns no retry because no isolated source alone is inconclusive. | Record required zero-overlap counts and the maximum normalized residual upper bound. When the data showed duplicated interval dependency, extend the existing midpoint-weight proposal to the all-source-pass/recombined-fail case; retain the untouched full-ball basis and incoming boundary as the only acceptance authority. | Confirmed. All 60 required residual balls contain zero. The weights-only and incoming-only normalized bounds are both `6.321836241995893e-9`, while their independently recombined full residual is exactly doubled to `1.2643672511747361e-8`. A new functional regression reproduces that dependency and proves the midpoint proposal is accepted only after its full-ball forward residual passes. The matching suite passes 61/61, and the former level-4 hop now clears. |
| D-13 | After D-12 clears level-4 matching, the second paired observable fails during lower terminal factorized-adjoint line contraction at epsilon power -2. The real radius is about `2^-25`, wider than the `1e-8` output target. The C++ layer throws typed `TerminalOutputInconclusive`, but only endpoint-batch commands translate that code; the paired-line command collapses it into a generic error, so the production retry driver cannot react. | Extend the explicit publication tolerance and typed terminal-output retry contract to paired integrated observables, not only endpoint limits. Preserve request index, functional, epsilon power, and required producer-level accuracy in the returned error. | Confirmed. The streamed and atomic pair paths now return `terminal_output_ball_inconclusive` with the exact request, functional, epsilon power, arm, and tile. Stream 4/4, native batch 27/27, runner 46/46, and the C++ pair test pass. The production driver performs the intended bounded retry. |
| D-14 | D-13's retry raised level 4 from 8 to 10 and 12 computation digits, but the terminal line radius did not improve and remained about `2^-24`. Tightening also reintroduced fully zero-overlapping internal match failures and drove levels 5/6 to 14/16. The accuracy check was executed on one terminal tile of one arm, before summing the other tiles and before the physical `-lower+upper` combination. | Move line publication certification from terminal tile contraction to the final atomic paired-line value. Retain every intermediate ball unchanged; certify every requested coefficient only after all tiles and both arms are combined. Endpoint limits remain certified at their actual final endpoint. | Partially confirmed. The failure is now correctly attributed to the final paired line at epsilon power 3, not an intermediate tile, and atomicity/typed retry are preserved. The final pair is nevertheless genuinely too wide with real radius `2^-24`; digit retries again leave that radius unchanged. Full log: `/var/folders/r9/t81xsghn0r198pktdqz_0v700000gn/T/diffexp2-double-box-timing.81ImZi/double-box.log`. |
| D-15 | The final level-4 paired-line radius is invariant under matching-digit retries, while working precision 150 already supplies far more arithmetic bits than the requested eight digits. The line is assembled from finite Taylor locals and terminal adjoint contractions at level Taylor order 25. | Controlled profile test only: keep D-11 digits `<|5->12,6->14|>` and level-6 Taylor 50 fixed, raise only level-4 Taylor order from 25 to 50, and leave all publication targets unchanged. | Rejected. The first final-pair failure moved from epsilon power 3 with real radius `2^-24` to epsilon power -1 with real radius `2^-25`: only one bit of improvement, still far outside the `1e-8` publication target. The ensuing digit escalation was the already-rejected D-14 path, so the run was stopped before repeating it. No process remains. Full log: `/var/folders/r9/t81xsghn0r198pktdqz_0v700000gn/T/diffexp2-double-box-timing.bpXInW/double-box.log`. |
| D-16 | D-14/D-15 expose only the final combined radius, so it is still unknown whether one terminal tile dominates, each arm is independently wide, or `-lower+upper` duplicates correlated uncertainty. Retrying digits, Taylor order, or working precision without this attribution would repeat disproved sweeps. | Diagnostic-only change: construct pair conditioning before publication certification and retain lower/upper/combined and per-tile radius exponents in both atomic and streamed typed failures. Run the D-11 baseline profile exactly once and stop after its first final-pair failure. | Confirmed. At the failing epsilon power 3, lower terminal tile 1 alone has real radius `2^-24`; lower tile 0 is `2^-28`. The eleven upper tiles are all `2^-35` or tighter, with two effectively exact. The combined radius is therefore inherited from one lower terminal factorized contraction, not duplicated lower/upper uncertainty. Atomic and streamed failure regressions pass and no acceptance changed. Full log: `/var/folders/r9/t81xsghn0r198pktdqz_0v700000gn/T/diffexp2-double-box-d16.r5Zvrf/double-box.log`. |
| D-17 | D-16 localizes the width to the lower terminal factorized tile, whose production value uses the retained factorized adjoint `z^T u`. It is unknown whether the adjoint solve itself widens a value that direct factorized contraction `J y` can enclose tightly, or whether both routes inherit the same incoming uncertainty. | Resume only the D-16 level-4 boundary, enable the existing direct-factorized/adjoint comparison plus terminal sensitivity diagnostics, and record midpoint and radius exponents for both routes. Do not change the authoritative route or retry any accuracy setting. | Confirmed architectural mismatch. At epsilon power 3, direct `J y` is `-20.08264305699477` with radius `2^-30`; adjoint `z^T u` is `-20.08312640954645` with radius `2^-24`. Their discrepancy is about `4.83409e-4`, far beyond both enclosures. The adjoint linear solve itself has maximum forward residual about `3.51e-301`, so the discrepancy is the accepted match residual composed through the terminal functional. A local match-relative certificate is not a terminal forward-error certificate. Full diagnostic log: `/var/folders/r9/t81xsghn0r198pktdqz_0v700000gn/T/diffexp2-double-box-d17.1eSZgd/double-box.log`; the reused boundary was explicitly stale-accepted for diagnosis only and is not acceptance evidence. |
| D-18 | D-17 proves neither direct `J y` nor the ordinary pointwise adjoint can be selected merely by local enclosure width: direct is tight but does not reproduce the incoming solution to public accuracy, while the adjoint exposes that amplified residual. The code already has an ODE-composed backward adjoint with an explicit Taylor-tail certificate. | On the same diagnostic-only level-4 resume, run the composed adjoint in report mode once. Record applicability, certified output-tail bound, recurrence contraction, and discrepancy from the ordinary adjoint. Do not make it authoritative yet. | Not applicable in its current implementation. The lower terminal row is rejected as `row-requires-laurent-log-center-adjoint` with `forcing_power=0`; no composed value or tail certificate is produced. This is now a code/mathematics audit target rather than a reason to select the direct fallback. Full diagnostic log: `/var/folders/r9/t81xsghn0r198pktdqz_0v700000gn/T/diffexp2-double-box-d18.bjiRF1/double-box.log`. |
| D-19 | With the center-pole composed route unavailable, the remaining pointwise adjoint width could be either retained incoming-ball uncertainty or a missing finite-Taylor tail. The existing physical-tail theorem can distinguish them and quantify the terminal amplification coefficient by coefficient. | Reuse the diagnostic-only level-4 boundary once with `DE2_DIAGNOSTIC_TERMINAL_PHYSICAL_TAIL=report` and terminal sensitivity reporting. Record the certified incoming tail and its propagated output bounds; do not inflate or change publication. | Inconclusive without changing acceptance. The regular physical-tail model is present, but none of its sixteen deterministic dyadic witness radii certifies, so it reports `no-certified-dyadic-witness` and supplies no bound to inflate or plan from. The retained pointwise adjoint still reproduces D-16 exactly. Full log: `/var/folders/r9/t81xsghn0r198pktdqz_0v700000gn/T/diffexp2-double-box-d19.cZVX75/double-box.log`. |
| D-20 | D-19 cannot separate visible incoming-ball width from an unseen tail. The pointwise adjoint result is an explicit epsilon convolution, so its visible enclosure can instead be attributed exactly to `(component, adjoint power, incoming power)` terms. | Add opt-in coefficientwise convolution-radius attribution under the existing terminal-state diagnostic and replay only the D-16 level-4 boundary once. Record the dominant source term for the first failed public coefficient. | Confirmed. For lower terminal tile 1 at failed output ε^3, component 0 dominates. Its adjoint ε^-1 coefficient is `-6.85194410105` with real radius `2^-1017`, while the retained incoming ε^4 coefficient is `39.5658955845` with real radius `2^-27`; their product has radius `2^-25`, and summing component-0 convolution terms gives the observed `2^-24` component result. Component 2 is only `2^-31`, and component 1 is exactly zero. Thus the visible width originates in the retained incoming local enclosure, not the adjoint solve, GCD, working precision, or lower/upper correlation. Full diagnostic log: `/tmp/diffexp2-double-box-d20.fd8yMA/double-box.log`. The first setup-only launch used an absent FIRE path and never reached transport: `/tmp/diffexp2-double-box-d20.PUYonG/double-box.log`. |
| D-21 | D-20 shows that the failing lower arm has only one match, so its wide incoming value is the level-4 anchor published by level 5. The D-14 retry log reveals that raising level 4 from 8 to 10 digits auto-resumed the old level-4 boundary because Boundary checkpoint validation binds only level-5 `MatchingDigits`, not the downstream `PublicationDigits` used to create that boundary. The same stale reuse occurred when level 4 rose from 10 to 12. Therefore D-14 never actually tested a level-4 anchor published at 12 digits. | Bind every Boundary and transport checkpoint to its explicit downstream publication digits, retain compatibility only when a legacy checkpoint's implied default is exactly correct, and add a loader regression proving that a consumer-digit increase invalidates an otherwise matching boundary. Then perform one controlled double-box run which genuinely rebuilds the anchor at the requested publication accuracy. | Confirmed checkpoint defect; rejected as the numerical limiter. The loader regressions pass (checkpointing 44/44 and native dispatch 47/47). A clean run rebuilt levels 6 and 5 and wrote a genuinely 12-digit-published level-4 boundary, but the final real radius remained `2^-24`; only the imaginary radius improved, from roughly `2^-26`/`2^-29` to `2^-36`. Publication-digit retries therefore cannot remove the dominant real enclosure. Full log: `/tmp/diffexp2-double-box-d21.eB6u6P/double-box.log`. |
| D-22 | A correctly republished anchor still reaches the lower terminal match with component-0 ε^4 radius `2^-27`. It is unknown whether this radius is already present in one stored anchor Taylor coefficient or is created by evaluating that local at the planner's direct regular-to-singular handoff. | Dump the exact level-4 plan and add diagnostic-only Taylor-term radius attribution for the one incoming coefficient. Do not repeat working-precision, digit, or global Taylor-order sweeps. | Confirmed evaluation-distance wrapping. The lower plan contains only the anchor at `11/23` and the singular center at zero; their balanced handoff evaluates the anchor at local `t=-9/29`. The retained ε^4/component-0 Taylor coefficient radii grow from `2^-42` at order 0 to `2^13` at order 25. Multiplication by `(-9/29)^n` suppresses the order-25 term to radius `2^-29`, but the sum of all 26 interval terms becomes exactly the authoritative `2^-27` radius. The upper terminal evaluates at only `46983/2896775` and remains `2^-38`. Plan log: `/tmp/diffexp2-double-box-d22-plan/plan.log`; attribution log: `/tmp/diffexp2-double-box-d22-eval3/double-box.log`. |
| D-23 | D-22 proves that one long regular-anchor evaluation, rather than publication or terminal algebra, creates the dominant interval width. | Reuse the retained physical q/C equation to certify the source only to an intermediate ordinary point, evolve a new local there, and evaluate the remaining shorter segment. First run this in diagnostic/report mode; make it authoritative only if both tail certificates and the final value enclosure are rigorous. | Rejected for the lower terminal. Both half-step tail theorems formally certify, but their conservative bounds are unusable: the source-half tail maximum is about `3.10e42`, the return-half maximum is about `8.00e64`, and the inflated ε^4 result has radius about `2^201`. The same construction is well behaved on the already-easy upper terminal, so the implementation is sound but the lower q/C norm majorant is too pessimistic. Log: `/tmp/diffexp2-double-box-d23-report2/double-box.log`. |
| D-24 | The lower source is a linear ordinary recurrence. Its expanded Taylor coefficient balls repeatedly contain the same uncertain center value, and D-22's direct sum treats those correlated copies as independent. | Build the retained finite Taylor transfer operator from zero-radius center impulses, then apply that operator once to the original center-value balls. Compare this factorized evaluation with the direct stored-local evaluation; require a certified q/C prefix replay, full overlap, identical retained epsilon coverage, and a bounded operator size before selecting it. | Confirmed and implemented, but it exposes the same dependency in the preceding ordinary tile integral. The ε^4/component-0 incoming radius tightens from `2^-27` to `2^-40` in about 0.25 seconds, with the same midpoint and overlapping balls. The final ε^3 line radius improves from `2^-24` to `2^-28`, so the former failure clears. The first remaining failure moves to ε^9 with radius `2^-21`; attribution shows the terminal tile is now only about `2^-30`, while ordinary lower tile 0 retains radius `2^-21`. Unit and focused persistent tests pass. Diagnostic log: `/tmp/diffexp2-double-box-d24-factorized/double-box.log`; production replay: `/tmp/diffexp2-double-box-d24-production/double-box.log`. |
| D-25 | D-24 proves that terminal value evaluation and the adjacent ordinary stored-row line integration independently expand the same uncertain center value into correlated Taylor coefficient balls. Once terminal evaluation is factorized, lower tile 0 becomes the sole dominant width at ε^9. | Apply the same certified finite-transfer construction to ordinary stored-row integration: integrate unit-impulse recurrence columns first, then apply the retained center-value balls once. Preserve the direct integration's branch/divergence/tail metadata, require coefficientwise overlap, and retain the same bounded operator cap. | Confirmed. The reusable line-transfer primitive overlaps the historical direct finite integral, preserves its metadata, and is selected only after a certified q/C prefix replay; unsupported, divergent, or over-cap cases retain the direct path. The focused C++ suites pass 5/5, checkpointing passes 44/44, and native dispatch passes 47/47. A controlled level-4 replay completed all seven observables in 48.29 seconds and wrote the level-3 boundary instead of failing at ε^9. Log: `/tmp/diffexp2-double-box-d25/double-box.log`. |
| D-26 | The D-25 level-3 boundary passes checkpoint schema/configuration validation, but resuming it immediately fails before transport because its supplied relative epsilon gauge does not match the freshly reconstructed level-3 matrix. | Inspect the serialized boundary basis/gauge and compare it with the uninterrupted level transition. Determine whether the writer stores the producing level's gauge where the lower consumer's gauge is required; do not rerun numerical accuracy settings. | Confirmed planner/runtime contract bug and fixed. The native planner deliberately returns its already gauge-normalized matrix, while `ft2NormalizeEpsilonBasis` accepted only the pre-normalization input hash. Zero-gauge levels 4--6 hid the inconsistency; level 3 is the first nontrivial gauge. The normalization seam now accepts either exact bound hash while still checking the gauge identity and normalized payload hash. The new composed regression passes in the 24/24 preplanner suite and checkpointing remains 44/44. The first launch was rejected only because an uncommitted worktree is deliberately marked unversioned; the explicit opt-in then reproduced the gauge mismatch. Failure log: `/tmp/diffexp2-double-box-d26/gauge-mismatch.log`; post-fix run: `/tmp/diffexp2-double-box-d26/double-box.log`. |
| D-27 | With D-26 fixed, level 3 reaches its first lower regular-to-singular match. The midpoint passes and all 133 required residual balls contain zero, but incoming-only and weights-only probes are each inconclusive at about `1.53e-7`; their recombined residual is almost exactly doubled at `3.06e-7`. The generic digit retry would require a rejected 14/16/18 producer ladder and stops at its configured cap. | Trace whether this match still evaluates the ordinary incoming local by independently summing widened Taylor coefficients. Reuse the D-24 certified q/C factorized evaluation at the general matching seam if applicable, with the same cap and full-overlap authority; do not raise digits. | Rejected as the limiter. The certified factorized path was selected for all seven basis columns and the incoming local, but the residual and isolated probe bounds were unchanged (`3.05504245e-7` combined and about `1.527521e-7` each). The width is therefore already present in the cross-level boundary and is amplified by this match, rather than being introduced by this point evaluation. The diagnostic also exposed a performance defect: the same 140-column transfer was rebuilt for every basis column and for 26 prefix/probe attempts, consuming about ten minutes before reproducing the same failure. Do not repeat until transfer reuse or a tighter targeted producer test is in place. Log: `/tmp/diffexp2-double-box-d27/double-box.log`. |
| D-28 | D-27 local factorization cannot tighten the cross-level boundary. The retry classifier requests the specific producer ladder `<|4->14,5->16,6->18|>`, but the generic retry guard stops at 16 before testing it. D-21's negative digit result predates the D-24/D-25 dependency fixes, so it does not establish whether this now-targeted producer ladder remains ineffective. | Perform one clean producer run with exactly the requested 14/16/18 profile, level-6 Taylor order 50, and all other settings fixed; stop after publishing the level-3 boundary. Then replay only the first level-3 match. Do not broaden the default retry cap unless this controlled test succeeds. | Rejected before producing a boundary. Deepest level 6 fails its third lower hop at an internal `1e-18` contract: the midpoint passes, all required residual balls contain zero, and the maximum bound is about `6.43e-17`. Basis-only passes while incoming/weights are each about `3e-17`. Thus broadening the cap to 18 would merely overconstrain a hop that already meets a 16-digit publication contract. Log: `/tmp/diffexp2-double-box-d28/producer.log`. |
| D-29 | D-28 shows the generic transitive ladder charges a two-digit internal guard to the deepest producer even when level 6 only has to publish a 16-digit boundary. A level-6 computation/certification target of 16 already encloses the observed `6.43e-17` hop residual, while level 5 can still compute at 16 and publish the 14-digit boundary needed by level 4. | Controlled profile only: use `<|4->14,5->16,6->16|>` with level-6 Taylor 50, holding all other settings fixed. Stop after the level-3 boundary and replay its first match. If successful, replace the blindly transitive digit raise with a publication-driven producer plan rather than increasing the global cap. | Rejected before producing the level-3 boundary. Level 6 completes at 16 digits in 10.71 seconds, but level 5's first lower match at its `1e-16` internal contract is inconclusive. Its basis probe passes at about `6.82e-25`, while incoming reaches about `1.95e-16`, weights about `1.25e-15`, and the combined residual about `1.38e-15`; the driver therefore requests the already-rejected level-6 target 18. Together D-28 and D-29 close both naive digit ladders: 18 overconstrains level 6, while 16 does not certify level 5. Do not try another digit-profile permutation; the next change must address the certification/uncertainty composition itself. Log: `/tmp/diffexp2-double-box-d29/producer.log`. |
| D-30 | D-29's midpoint residual passes at `6.82e-25`, all 42 required residual balls contain zero, and only the interval association is inconclusive. The normal-frame solver applies the uncertain incoming series once through its solved weights and independently again in `A*w-b`; at a same-digit producer/consumer seam this spends the entire enclosure twice. | Add a bounded factorized-RHS proposal for a complete authoritative residual system. Solve fixed unit-coefficient responses in midpoint arithmetic, form each full-ball operator defect `I-A*T`, then apply every original incoming coefficient ball exactly once. Require the unchanged full-ball basis and incoming series to certify the result; a midpoint pass and non-failing source probes only authorize the proposal, never publication. Replay only D-29's preserved level-5 boundary first. | Confirmed for both matching frames. The first replay correctly made no change because the singular match lacked an exact fused Rational shadow. Extending the same certificate to the honest finite physical operator `F*R*T` clears that lower singular match in about 27 ms. The next replay reached an ordinary upper match with the same doubled enclosure (`1.95e-16` from each source and `3.89e-16` combined); the identity-frame version clears it too. Level 5 then completes all matches and line contractions before reaching endpoint publication. The primitive has a positive dependency fixture and a negative full-ball-operator fixture; matching is 62/62 and the focused persistent suites pass. Logs: `/tmp/diffexp2-double-box-d30/level5-finite-physical.log` and `/tmp/diffexp2-double-box-d30/level5-all-matches.log`. |
| D-31 | After D-30, level 5 reaches its upper endpoint but ε² has radius `2^-45`, roughly three times its `1e-14` publication allowance. The endpoint adjoint already removes match-weight dependency, but it first evaluates the incoming ordinary Taylor local coefficientwise and then contracts those coefficients, potentially repeating the same uncertain center balls. | Retain every unit-center response from the certified D-24 ordinary transfer, compose each response with the endpoint adjoint first, apply the original center balls once, require full coefficientwise overlap with the direct contraction, and select only the composed enclosure. | Rejected as the remaining limiter. The composed contraction is selected for the lower line terminal, upper line terminal, and upper endpoint, but the endpoint ε² radius remains exactly `2^-45`. Thus the width is already present in the independent center/boundary coefficient balls, not introduced by Taylor evaluation or adjoint convolution. Physical-ODE evolution, matching, and focused persistent suites pass 6/6. Log: `/tmp/diffexp2-double-box-d30/level5-composed-adjoint.log`. |
| D-32 | D-28 rejected the 14/16/18 producer ladder because level 6's third lower match could not certify its `1e-18` enclosure, while D-30 now removes precisely that solve/RHS dependency in both singular and ordinary matching frames. A genuinely tighter level-6 boundary is the remaining identified source for D-31's level-5 endpoint width. | Repeat the exact D-28 14/16/18 producer profile only after D-30, with level-6 Taylor 50 and all other settings fixed. Stop after the level-3 boundary. If level 6 still fails with the factorized operator selected, reject digit escalation permanently; if it completes, test whether level 5's endpoint and the level-3 consumer now pass. | Partially confirmed, but rejected as a complete fix. D-30 clears the former D-28 match: level 6 completes at 18 digits in 33.65 seconds and publishes a new level-5 boundary. Level 5 then completes matching and reaches the endpoint, but its real radius remains exactly `2^-45`; the first failure moves only from ε² to ε¹. Therefore a tighter producer solve alone does not remove the retained endpoint width, and raising level 6 to 20 would be an unsupported repeat. Log: `/tmp/diffexp2-double-box-d32/producer.log`. |
| D-33 | Level-5 endpoint radius `2^-45` is invariant between a 16-digit and a genuinely rebuilt 18-digit level-6 producer, so either the dominant center coefficient is set by another fixed-width operation or the boundary publication path discards the extra producer accuracy. | Resume only D-32's new level-5 boundary with terminal coefficient/radius attribution. Identify the exact `(component, adjoint power, incoming power)` source and compare it with D-31; inspect checkpoint publication binding before changing any digits, Taylor order, or precision. | Confirmed a non-monotone internal-match effect, not checkpoint publication loss. The new failure at endpoint ε¹ is dominated by component 0, adjoint ε^-1, incoming ε². In the 18-digit run that incoming coefficient has radius `2^-45`; in the 16-digit run the same coefficient had the same midpoint but radius `2^-48`. Thus tighter requested matching activates additional rigorous factorized match fallbacks whose flattened weight balls are wider downstream. The extra producer accuracy is present, but correlation is lost before endpoint publication. Log: `/tmp/diffexp2-double-box-d33/level5-attribution.log`. |
| D-34 | D-33 shows that forcing every internal hop to carry the two safety digits needed for computation can worsen the final enclosure. A level's final boundary/endpoint publication is independently certified at the downstream contract, so internal hops need only certify the digits actually published; the higher computation target may still tighten arithmetic and source construction. | Diagnostic profile only: keep computation digits 14/16/18 but set the internal certification safety guard from 2 to 0, yielding level-6 computation/certification/publication 18/16/16 and level-5 16/14/14. Preserve every final publication check and all other settings. If this does not improve the endpoint, revert the override and do not weaken production. | Confirmed through the complete producer segment. Level 6 completes in 25.32 seconds at computation/certification/publication 18/16/16. Level 5 clears its formerly invariant endpoint and publishes all three observables in 51.83 seconds at 16/14/14. Level 4 publishes all seven observables in 198.03 seconds and writes a new level-3 boundary. No publication tolerance was weakened; each final endpoint/line coefficient still passed its downstream 14- or 8-digit authority. Log: `/tmp/diffexp2-double-box-d34/producer.log`. |
| D-35 | D-34 produces the first level-3 boundary built with higher computation accuracy but publication-bound internal hop certification. D-27's first level-3 match failed at about `3.06e-7` against `1e-8` using the older boundary. | Resume exactly D-34's level-3 boundary under the same zero-guard certification policy and stop after publishing level 2. First verify that the former first-match failure clears; then let the bounded level finish unless a new typed failure appears. | Partially confirmed. The former first-match failure clears and the seven-dimensional level runs its complete arm for about 3.5 minutes. The remaining failure is the final paired line at ε¹, with radius `2^-22` against the 8-digit publication target; the driver correctly requests level-3 computation digits 10. Its automatic retry is incorrectly blocked because the global max-cap check sees the already-seeded level-6 value 18 and compares it to the default cap 16, even though the newly raised level is only 10. Log: `/tmp/diffexp2-double-box-d35/level3.log`. |
| D-36 | D-35 reaches final publication at level 3 and asks for the ordinary two computation guard digits. Under the zero internal-certification guard this means computation/certification/publication 10/8/8, so the retry need not tighten any already-cleared hop. | Resume D-34's same level-3 boundary with seeded digits `<|3->10,4->14,5->16,6->18|>` and stop after level 2. Do not rebuild upstream levels. Separately fix the retry ceiling to reject only newly requested values beyond its allowed ceiling, not pre-approved seeded values. | Rejected as an incomplete producer plan. Checkpoint validation correctly rejects the old level-3 boundary because it was published at 8 digits, not the newly required 10. Resuming the still-valid level-4 boundary avoids rebuilding levels 6/5, but level 4 at computation/certification/publication 14/10/10 reaches its final paired line and fails ε⁷ with radius `2^-29`; it requests computation 16. The retry therefore legitimately needs a newly republished upstream ladder, not merely a level-3 rerun. Logs: `/tmp/diffexp2-double-box-d36/level3.log` and `/tmp/diffexp2-double-box-d36/level4-republish.log`. |
| D-37 | D-36's final publication check requests the exact transitive computation profile `<|3->10,4->16,5->18,6->20|>`. With zero internal safety guard, the corresponding hop certifications are publication-bound rather than computation-bound: level 6 certifies 18, level 5 certifies 16, level 4 certifies 10, and level 3 certifies 8. | Run one clean producer through the level-3 boundary with that exact profile, level-6 Taylor 50, and no other changes. This is the first test of the requested 20-digit computation under the corrected certification architecture; do not infer a higher target if it fails. | Rejected at the deepest producer, before any downstream checkpoint. Level 6 finishes its native transport in about 26 seconds but cannot publish the first paired observable at the 18-digit boundary contract: ε⁰ has midpoint `0.3108456323658372` and real radius `2^-60`. The retry driver asks for level-6 computation 22, but that escalation is not inferred because this experiment explicitly capped the evidence at 20. The next step is coefficient/tile attribution of this final level-6 enclosure, not another digit profile. Log: `/tmp/diffexp2-double-box-d37/producer.log`. |
| D-38 | D-37 moves the blocker to the deepest level's final paired-line publication. The typed conditioning record already contains lower/upper and per-tile balls, but the full one-line diagnostic is too large to identify the dominant ε⁰ source safely by inspection. | Reproduce only level 6 with the same D-37 profile in diagnostic/report mode and extract the ε⁰ lower/upper/per-tile radius attribution. Do not change digits, Taylor order, working precision, or acceptance. Use the result to distinguish an ordinary integration dependency from an endpoint/arm-combination dependency before editing code. | Confirmed an arm-aggregation budget issue, not a missing local factorization. All nine nonterminal lower tiles select the certified factorized ordinary-row path. At ε⁰, lower tiles 4, 6, and 8 each have real radius `2^-62`; the terminal tile is `2^-63`, the upper arm is tighter, and independently summing the lower tiles reaches `2^-60`. The regular value hops were already certified at computation digits plus the global two safety digits, so the remaining loss is the approximately three-decimal-digit observable amplification plus the finite tile sum. Log: `/tmp/diffexp2-double-box-d38/attribution.log`. |
| D-39 | D-38 shows that one tolerance currently serves two differently conditioned operations. Publication-bound basis matching must keep the zero extra guard established by D-33/D-34, but regular value recentering is later amplified by stored-row functionals and summed over an arm. Its existing computation-plus-two safety digits leave the D-37 ε⁰ aggregate only narrowly inconclusive. The classic `DigitBudget` already accounts for this with `Ceiling[Log10[number of segments]]`; the native value-handoff path omitted that term. | Separate regular-value-handoff budgeting from matching certification. Add the classic aggregation guard `Ceiling[Log10[lower tiles + upper tiles]]` only to regular value recentering, while retaining zero extra matching guard and unchanged computation, Taylor, precision, and publication contracts. First test level 6 only. | Confirmed. The focused transport suite passes 80/80 and the native independent-arm fixture passes. With 14 tiles, the native value path adds two aggregation digits; level 6 publishes both 18-digit boundary observables and writes the level-5 checkpoint in 27.01 seconds, versus 25.32 seconds for D-34's lower-accuracy level-6 run. The formerly failing ε⁰ midpoint is unchanged. Log: `/tmp/diffexp2-double-box-d39/level6.log`. |
| D-40 | D-39 is the first principled level-6 producer for D-36's requested 10/16/18/20 computation profile: matching remains publication-bound, while only regular recentering pays the established segment-sum guard. | Resume the newly written D-39 level-5 boundary and run levels 5 and 4 through publication of the level-3 boundary. Keep every profile and acceptance setting unchanged. If successful, replay level 3 at computation 10 from the new checkpoint. | Rejected at level 5 before a level-4 boundary. The first paired observable fails at ε⁴ with midpoint `40.91761399648162` and real radius `2^-47` against its 16-digit publication contract. The lower arm has only two tiles with radii `2^-50` and `2^-49`; the sixteen upper tiles are individually `2^-51` or tighter, but lower and upper aggregates both reach about `2^-48` before the final pair reaches `2^-47`. The automatic 20/22 digit request is not followed. Log: `/tmp/diffexp2-double-box-d40/levels5-4.log`. |
| D-41 | D-40 is not the D-38 many-ordinary-tile pattern: both complete arms contribute comparable uncertainty, and the two-tile lower arm is already wide. It is unknown whether the lower terminal match, upper value recentering, or shared-boundary correlation dominates. | Replay only level 5 from D-39 with terminal-state and factorized-row diagnostics enabled. Map the two lower and sixteen upper tiles to contraction routes and attribute ε⁴ in the dominant terminal tile. Do not alter accuracy or acceptance. | Confirmed inherited boundary width plus terminal conditioning, not an inaccurate adjoint solve or an unused factorized ordinary route. Lower tile 0 and upper tiles 0--14 already use certified factorized ordinary rows; both terminal tiles use certified factorized adjoints. At lower ε⁴, component 1 contracts adjoint ε^-1 (`-8.06222678571`) with incoming ε⁵ (`-2.87261871995`) whose radius is `2^-52`, producing about `1.64e-15` uncertainty. The adjoint residual is effectively exact. The upper terminal has the analogous conditioning. The final failure is only about 1.8 times its allowance, so one additional decimal digit is the next bounded test; the generic two-digit 20/22 escalation is unsupported. Log: `/tmp/diffexp2-double-box-d41/level5-attribution.log`. |
| D-42 | D-41 measures a one-decimal-digit publication deficit at level 5, while D-39 already produced level 6 at computation 20. The generic retry currently adds the global two-digit safety margin and then propagates it transitively, which would repeat the rejected overconstraint pattern as 20/22. | Clean controlled profile only: use computation digits `<|3->10,4->16,5->19,6->20|>`, zero extra matching-certification guard, the D-39 regular-value aggregation guard, and level-6 Taylor order 50. First require level 6 to publish the new 19-digit level-5 boundary, then run level 5 through its 16-digit publication. Do not infer 21 or 22 if either stage fails. | Rejected at the deliberately bounded first stage. Level 6 completes its transport, but ε⁰ of the first paired observable has midpoint `0.3108456323658372` and radius `2^-63`, sufficient for the prior 18-digit boundary but not the requested 19-digit boundary. The driver proposes 22 only because terminal retries still hard-code the global two-digit increment; it is blocked and not run. This proves that D-41's one-digit downstream deficit cannot be propagated naively as a one-digit upstream publication request. The next change is a typed backend publication-deficit field and deficit-aware, non-blind retry planning. Log: `/tmp/diffexp2-double-box-d42/level6.log`. |
| D-43 | Terminal-output failures expose midpoint and radius text but not the exact factor by which the rigorous ball exceeds its allowance. The Wolfram translator therefore always requests two extra digits, and the generic producer planner applies another two digits at every upper level, even after D-28--D-42 disproved such blind ladders. | Extend the C++ publication decision and typed terminal error with a conservative integer `required_additional_digits`, computed directly from the exact magnitude comparison. Preserve the existing fallback for old backends. Make the terminal retry use this field and add focused C++/Wolfram tests before changing any numerical profile. Separately keep propagated-matching retries on their existing contract until their planner has its own evidence. | Confirmed. The C++ decision computes the least decimal relaxation in exact FLINT magnitude arithmetic; terminal endpoint, atomic pair, and streamed pair errors serialize it. Unit cases pin deficits 0, 1, and 3; C++ matching is 62/62 and Wolfram dispatch is 47/47, including the old-backend fallback. Replaying D-42 reports `required_additional_digits -> 1`, so the driver requests level 6 at 21 rather than 22 and then stops at the configured cap without running it. Log: `/tmp/diffexp2-double-box-d43-contract/measured-contract.log`. |
| D-44 | D-43's broader persistent transport test failed while restoring a checkpoint: earlier factorized-authoritative-RHS work serialized two new refined-match fields while the strict restoration schema still admitted only the old field set. This could invalidate checkpoint resumes even though fresh transport worked. | Restore both factorized fields with strict type/consistency checks, while accepting old same-schema checkpoints that legitimately lack both fields and default to the historical non-factorized state. Rerun matching, checkpoint, deferred-checkpoint, and full atomic transport round-trip tests. | Confirmed. All four checkpoint-focused C++ tests pass, including the formerly failing terminal factorized transport save/restore. The compatibility branch rejects a half-present record and contradictory flag/count pair rather than weakening schema validation. |
| D-45 | D-40 measured a one-digit deficit at level 5, and D-42/D-43 separately prove that publishing its 19-digit input boundary requires one more level-6 computation digit beyond 20. The resulting profile `<|3->10,4->16,5->19,6->21|>` is therefore publication-driven at both seams rather than the old two-digit transitive guess. | Run level 6 once at computation/certification/publication 21/19/19 with Taylor 50, zero matching guard, and the regular aggregation guard. If it publishes, run level 5 at 19/16/16 from that new boundary. Do not infer any higher value if either certificate fails. | Rejected as evidence that the requested computation digit is not reaching the relevant numerical contract. Level 6 at computation 21 reproduces D-42 bit-for-bit: ε⁰ remains radius `2^-63` and still reports a one-digit deficit. No 22-digit run is inferred. Inspection shows `cppRegularValueRelativeAccuracyMaxExact[]` uses `Max[ResidTol, MatchTol]`; at WP 150, `ResidTol=10^-15` dominates every per-level matching target above 15, so raising 20 to 21 cannot tighten regular value recentering at all. Log: `/tmp/diffexp2-double-box-d45/level6.log`. |
| D-46 | D-45 identifies a tolerance-domain error: `ResidTol` is the fixed WP-derived ODE spot-check threshold, but it caps the certified regular-value handoff precision through `Max[ResidTol, MatchTol]`. Consequently, producer-accuracy retries above 15 digits are operationally ignored even though checkpoint metadata records the new digit. | Make the regular-value handoff accuracy follow the level's explicit `MatchTol`, with the existing safety and segment-aggregation guards. Keep `ResidTol` solely for ODE residual checks and leave the separate native center-tail geometry allowance unchanged. Add a regression proving consecutive high matching targets produce consecutive tighter handoff contracts, then rerun level 6 at the already-measured 21/19/19 profile. | Confirmed through the full upstream producer chain. Transport tests pass 81/81 and explicitly prove 20→21 tightens the handoff tolerance by 10×; the native independent-arm fixture passes. Level 6 now publishes 19 digits in 29.88 seconds, level 5 publishes 16 digits in 36.75 seconds, and level 4 publishes 10 digits in 133.47 seconds, writing a new level-3 boundary. The prior D-42/D-45 ε⁰ radius blocker and D-40 ε⁴ blocker disappear. Logs: `/tmp/diffexp2-double-box-d46/level6.log`, `/tmp/diffexp2-double-box-d46/level5.log`, and `/tmp/diffexp2-double-box-d46/level4.log`. |
| D-47 | D-46 supplies the first level-3 boundary whose entire producer chain both records and actually enforces the per-level accuracy targets. The previous D-35 level-3 run cleared matching but failed its final ε¹ paired line at radius `2^-22` against the eight-digit publication contract. | Resume the D-46 level-3 boundary at computation/certification/publication 10/8/8, with no other change, and require publication of level 2. Do not rebuild upstream levels or alter Taylor/precision if a typed failure appears. | Inconclusive under the explicit runtime bound. The level resumes cleanly, constructs its pole-free seven-dimensional epsilon basis with complete maximum 19, and emits no numerical failure, but does not finish the native batch within 300 seconds; the deadline terminates the entire process group and no process remains. The earlier D-35 run reached final publication in roughly 3.5 minutes, so this cannot yet be accepted as a no-regression result. Log: `/tmp/diffexp2-double-box-d47/level3.log`. |
| D-48 | D-47 gives no intermediate native-stage output, so it is unknown whether time is spent preparing the runtime matrix/bases, building the atlas, marching, or contracting. Repeating an uninstrumented five-minute run would not add causal evidence. | Replay only the beginning of level 3 from the same D-46 checkpoint with native stage timing enabled and a shorter diagnostic deadline. Record the last completed stage and compare with available D-35 diagnostics before changing precision, segmentation, or algorithms. | Confirmed a private value-handoff retry cost, not atlas preparation or a numerical failure. The atlas is ready by about 16 seconds, after which several regular hops exhaust Taylor orders `25,50,75,112,168,252,378,567,800` before falling back to the rigorous basis path. Other upper hops genuinely succeed only at orders 378, 567, or 800, so a blanket order cap would regress valid fast paths. The diagnostic reached final transport-state publication at about 120 seconds before its deliberate deadline. Log: `/tmp/diffexp2-double-box-d48/stage-timing.log`. |
| D-49 | D-48 distinguishes doomed and successful high-order retries only by their final outcome. An order cap cannot tell them apart and would remove valid 378/567/800 value hops. | Add a diagnostic-only exact per-attempt decimal deficit to the value-handoff trace, retaining the existing acceptance and retry ladder. Replay only enough level-3 hops to classify the sequence. | Confirmed a fixed 13-digit deficit at the public prefix on the first three doomed lower hops: every tested order from 25 through 800 reports the same integer deficit before the basis fallback. This supports progress-sensitive planning but not an integer-deficit cutoff, because improvements smaller than one decimal decade would be hidden. Focused C++ transport tests pass. Log: `/tmp/diffexp2-double-box-d49/deficit-trace.log`. |
| D-50 | D-49's integer deficit is too coarse for a safe non-progress rule. A synthetic impossible fixture remains far below its target yet its exact certified error shrinks at every order; stopping solely because the required decimal count is unchanged would discard real convergence. | Compare the exact worst radius/allowance ratio between consecutive private orders. Give direct-center and overlap phases independent histories; stop a phase only after two consecutive orders make no exact improvement. Keep the full ladder whenever the ratio continues to shrink, and pin that behavior with the existing impossible and adaptive high-order fixtures. | Confirmed as a selective performance fix. The persistent run-arms suite passes, including a genuinely improving impossible path that still reaches 800 and an adaptive path that succeeds only above its retained order. In the production level-3 trace, doomed hops stop at different evidence-driven points (for example 252 or 378) while intermittent exact improvements retain later attempts. Time from atlas readiness through the start of upper match 3 falls from about 53 seconds in D-48 to about 28 seconds in this diagnostic. No numerical acceptance or fallback was changed. Log: `/tmp/diffexp2-double-box-d50/progress-trace.log`. |
| D-51 | D-50 substantially shortens the known march bottleneck, but a complete level-3 publication has not yet been observed under the new rule. | Replay level 3 from the same D-46 boundary without diagnostic-print overhead, retain the exact profile, and impose a 180-second hard ceiling. | Inconclusive as a completion test and recorded as a timing failure. The kernel remains active at one full core with no numerical or retry failure, but does not publish the level-2 checkpoint by 180 seconds. The process group is terminated cleanly. Because stage timing was intentionally disabled, the next run must distinguish state publication from observable contraction; repeating this opaque run would add no evidence. Log: `/tmp/diffexp2-double-box-d51/level3.log`. |
| D-52 | D-51 stays CPU-active past 180 seconds after the march optimization, but gives no post-march stage evidence. | Repeat with stage timing and stop after enough of the first observable to measure state publication and per-tile contraction separately. | Confirmed that publication is not the blocker. The optimized march publishes both retained states in about 2.1 seconds (72.38--74.45 seconds). The first of ten observables then spends about 47 seconds contracting its 15 tiles serially (75.86--123.00 seconds), and the second immediately repeats the same tile loop. The old singleton streaming default therefore implies roughly eight minutes of contraction even though the march is now near one minute. Log: `/tmp/diffexp2-double-box-d52/post-march-stage.log`. |
| D-53 | D-52's ten observable contractions are independent, but both arms were deliberately serial and the singleton Wolfram chunks prevented any observable-level concurrency. | Add explicit bounded observable threads to the atomic pair contraction while keeping lower/upper arms serial, preserving request-order results and deterministic lowest-index failure. Test four-observable chunks/threads once; retain the singleton default in the lower-level API and expose bounded runner controls. | Confirmed both the performance path and the next numerical blocker. C++ pair/run-arm tests, the six-case Wolfram bridge, and native independent-arm integration pass; a three-observable native fixture proves two worker threads and stable output order. Production uses four cores (about 398% CPU) and about 2.1 GB RSS, reaching a typed terminal publication result in 143 seconds rather than timing out during the first two serial observables. Observable index 2 fails only at ε¹¹ with an exact one-digit deficit. The driver requests level-3 digits 11, then exposes the already-recorded D-35 ceiling bug by comparing pre-approved seeded level-6 digits 21 with its default cap 16. Log: `/tmp/diffexp2-double-box-d53/level3-parallel4.log`. |
| D-54 | D-53's requested level-3 increase 10→11 is below the retry ceiling, but the global cap rejects it because unchanged pre-approved upstream seeds 19 and 21 are already above that ceiling. | Compare the cap only against levels whose digit value is newly raised by the current retry. Preserve and permit unchanged seeded values; still reject any newly increased level that crosses the cap. | Confirmed by the focused runner suite (48/48). A fixture starting with upstream seeds above the default cap successfully raises only its lower producer by one digit while retaining all seeds; existing producer-ladder and retry tests remain unchanged. |
| D-55 | D-53 needs a level-3 producer retry, but rebuilding the complete upstream chain would obscure whether one additional level-4 publication digit can affect the failing terminal output. | Diagnostic only: reuse the explicitly stale-accepted D-46 level-4 boundary, rebuild level 4 at the one-digit-higher downstream contract, and publish a new level-3 boundary. | Level 4 republishes the boundary in 47.01 seconds, versus 133.47 seconds in D-46, under the new private-hop progress rule and observable concurrency. The checkpoint remains tainted by its stale-opt-in upstream source and is not acceptance evidence. Log: `/tmp/diffexp2-double-box-d55/level4-republish.log`. |
| D-56 | It is unknown whether D-53's one-digit final-output deficit is inherited from the level-4 boundary or generated inside level 3. | Run level 3 at computation digits 11 from D-55's one-digit-higher boundary, leaving publication, Taylor order, precision, and matching-certification contracts unchanged. | Rejected. Observable 2 at ε¹¹ reproduces the same midpoint, radius `2^(-18,-36)`, and one-digit deficit. The D-54 ceiling fix correctly accepts the proposed 11→12 retry; checkpoint validation then rejects the old level-3 boundary because its publication digits no longer match. A blind upstream publication increase is therefore closed. Log: `/tmp/diffexp2-double-box-d56/level3.log`. |
| D-57 | D-56 proves that the dominant width is generated within level 3, but per-tile radii alone do not identify which retained coefficient and terminal functional amplify it. | Replay once with terminal coefficient attribution and stop after the dominant lower terminal tile is reported. Do not alter any numerical setting or acceptance. | Confirmed. Lower terminal tile 5 dominates ε¹¹. Component 5 contracts adjoint ε^-3, midpoint about `-24269.5310400`, with incoming ε^14, midpoint about `99.1266153601` and radius `2^(-34,-55)`, producing the component output radius `2^(-18,-35)`. The upper terminal tile is much tighter. Thus the one-digit deficit is a terminal-functional conditioning problem acting on the immediately incoming level-3 coefficient, not an upstream level-4 boundary deficit. Log: `/tmp/diffexp2-double-box-d57/terminal-attribution.log`. |
| D-58 | D-57 suggests that the final singular match may need one extra private digit because its terminal functional amplifies the incoming uncertainty by about `2.4e4`. | Diagnostic only: set the existing terminal-match refinement control from the ordinary eight-digit certification target to nine digits, leaving the level-3 input boundary and every other setting unchanged. | Rejected. The final paired output is bit-for-bit unchanged and again fails observable 2 at ε¹¹ with radius `2^(-18,-36)` and a one-digit deficit. The terminal match still succeeds, so merely tightening its acceptance threshold cannot shrink the already-retained incoming ball. The next investigation is the penultimate incoming handoff that creates that ball, not another terminal-threshold or upstream-boundary retry. Log: `/tmp/diffexp2-double-box-d58/terminal-match9.log`. |
| D-59 | Reconstructing D58 from its checkpoint initially produced a different epsilon plan and much shorter chart chain. Continuing would have mixed configurations and invalidated any comparison. | Stop both setup launches before contraction; recover the exact private halo vector and explicit value-transport environment from the checkpoint and production timing gate. | Configuration error only, with no numerical conclusion. The exact private halos are `{2,5,1,0,0,0}`. The runner parses value transport as enabled, but the line planner also reads the explicit `DE2_VALUE_TRANSPORT=1` environment value; omitting it changed segmentation. The aborted logs are `/tmp/diffexp2-double-box-d59/preterminal-match9.log` and `/tmp/diffexp2-double-box-d59-div5/geometry.log`. |
| D-60 | Applying a nine-digit threshold to both penultimate matches under the mis-reconstructed geometry caused an upper-arm one-order epsilon-reservoir failure, even though D58's upper arm passed. | Scope the diagnostic control to the lower arm selected by D57 and first require an identical epsilon-plan identity and chart geometry. | Configuration error only, again with no output conclusion. The upper failure was caused by the different chart plan, not the lower hypothesis. It correctly requested one private level-3 epsilon order and was not followed. Logs: `/tmp/diffexp2-double-box-d60/lower-preterminal-match9.log` and `/tmp/diffexp2-double-box-geometry-div4/stage.log`. |
| D-61 | With the exact D58 configuration restored, it remains possible that the lower penultimate match's eight-digit certification creates the ε^14 ball amplified by the terminal functional. | Tighten only lower match 4 from eight to nine digits. Preserve computation digits, input boundary, upper arm, private epsilon plan, Taylor order, and precision; use stage timing to prove the `{6,10}` computation-11 chart plan. | Rejected. Lower match 4 accepts the stricter certificate and both arms publish normally, but observable 2 at ε¹¹ is bit-for-bit identical to D58: the same midpoint, radius `2^(-18,-36)`, and one-digit deficit. A stricter residual threshold does not refine the already-enclosed numerical sources. Log: `/tmp/diffexp2-double-box-d61/lower-preterminal-match9.log`. |
| D-62 | D56 was interpreted as evidence that an extra level-4 publication digit did not affect level 3, but D58/D61 now show that changing acceptance thresholds alone leaves deterministic interval inputs unchanged. | Load the D46 ten-digit and D55 eleven-digit level-3 boundary checkpoints directly and compare every stored numerical coefficient and its accuracy. | Corrected the earlier interpretation. The two `7 x 20` boundary arrays are exactly identical and have the same minimum decimal accuracy (`9.632959861247398`); D55 changed publication metadata but did not regenerate tighter balls. Therefore D56 did not disprove a genuinely tighter upstream numerical boundary. The next bounded test must rebuild the entire one-digit-tighter producer chain, not relabel the same boundary. |
| D-63 | D62 shows that level 3 needs a genuinely tighter numerical anchor. The nearest measured producer chain was level 6/5/4 computation digits `21/19/16`, publishing `19/16/10` digits respectively. | Increase that complete producer chain by one digit: computation profile `<|4->17,5->20,6->22|>`, publications `11/17/20`, Taylor 50 only at level 6, and otherwise the D46/D61 settings. Stop after the new level-3 boundary. | Confirmed. Level 6 publishes in 13.32 seconds, level 5 in 28.76 seconds, and level 4 in 79.26 seconds. The new boundary is numerically different: worst stored accuracy improves from `9.632959861247398` to `10.837079843890685` decimal digits (about 1.20 digits). The exact current source/configuration identity is retained. Log: `/tmp/diffexp2-double-box-d63/upstream-plus1.log`; checkpoint: `/tmp/diffexp2-double-box-d63/checkpoints/double_box_planar_level3_boundary.mx`. |
| D-64 | The D63 anchor is the first actual numerical test of the one-digit producer retry that D53 requested. | Resume level 3 at computation digits 11 from the exact D63 checkpoint, with no diagnostic matching override, four observable workers, and a 240-second hard ceiling. | Inconclusive only as a completion/timing result. No transport or numerical failure appears, but the run does not finish all ten observables before 240 seconds and is terminated cleanly. It runs beyond the old first-chunk failure time, but without stage timing that is not enough to claim the ε¹¹ blocker cleared. The next run increases only bounded observable concurrency and records stages; it does not rebuild or alter numerical inputs. Log: `/tmp/diffexp2-double-box-d64/level3-tight-anchor.log`. |
| D-65 | D64's four-observable chunks require roughly three serial contraction waves and hide which wave is active. | Keep the D63 numerical state unchanged, use eight bounded observable workers/chunk entries, enable stage timing, and retain a 300-second ceiling. | Confirmed that the old numerical blocker is cleared and exposed a distinct late-observable bug. March/state publication finishes at 74.73 seconds. The first eight observables—including old failing request 2—complete without a publication error. In the final two-observable chunk, upper terminal tile 9 throws `factorized incoming adjoint contraction changed its required epsilon coverage` at 250.58 seconds. This is an internal factorized-reassociation coverage invariant, not an accuracy retry. Peak observed RSS is about 2.5 GB. Log: `/tmp/diffexp2-double-box-d65/level3-threads8.log`. |
| D-66 | The first replay command after D65 reconstructed `FT_EPS_ORDER=5` from the diagnostic ledger's `DownstreamRawTop=5`. | Require the checkpoint's native-plan identity to agree before any computation and distinguish the public request from its derived internal reservoir. | Configuration rejected before transport: the D63 checkpoint uses public `FT_EPS_ORDER=0`; `5` is the derived level-3 downstream raw edge. No numerical hypothesis was exercised. Log: `/tmp/diffexp2-double-box-d66/level3-zero-columns.log`. |
| D-67 | Restoring public ε order 0 reproduced D65's native epsilon-plan identity, but the resume validator still rejected the launch on the separately persisted deep-boundary surplus. | Read the checkpoint record itself rather than infer omitted runner settings from the execution log. | Configuration rejected before transport. The exact D63 record uses `BoundaryExtraOrder -> 16` (with public ε order 0); the default is 4. No numerical hypothesis was exercised. Log: `/tmp/diffexp2-double-box-d67/level3-zero-columns.log`. |
| D-68 | D65's final two observables fail because factorized adjoint reassociation includes every unit-response column in an intersection-based epsilon-frame sum, even when that column's retained source amplitude is exactly zero. Its finite response window can therefore create a false coverage loss although zero times that response is the exact zero series at every epsilon order. | Move the factorized adjoint contraction into a focused helper, skip only amplitudes that Acb proves exactly zero, represent an all-zero transfer through the required output edge, and add a unit fixture where a short zero-amplitude response would otherwise truncate an active column. Retain detailed direct/factorized windows in any remaining invariant failure. Replay level 3 from D63 with identical numerical settings and stop after boundary publication. | Confirmed. The focused physical-ODE fixture and five affected C++ suites pass. The real march publishes at 73.77 seconds; all ten paired contractions, including D65's final two, finish at 243.44 seconds; export completes at 258.89 seconds and writes the current-source level-2 boundary. No accuracy retry or epsilon-depth change was needed. This replay explicitly accepted the numerically exact but source-stale D63 producer checkpoint after the contraction-only source edit, so it proves the bug fix but is not the eventual clean-from-scratch acceptance run. Log: `/tmp/diffexp2-double-box-d68/level3-zero-columns.log`; checkpoint: `/tmp/diffexp2-double-box-d68/checkpoints/double_box_planar_level2_boundary.mx`. |
| D-69 | The first level-2 resume omitted stale-checkpoint opt-in because D68's boundary was written by the current source. | Inspect the fingerprint and taint rules before weakening provenance. | Configuration rejected before transport. The source fingerprint is unchanged; D68 correctly propagates `Tainted -> True` from its explicitly stale-accepted D63 ancestor. A current writer does not erase unverified ancestry. Continue diagnostic resumes with explicit opt-in, and reserve untainted acceptance for a clean-from-scratch run. Log: `/tmp/diffexp2-double-box-d69/level2.log`. |
| D-70 | With the false factorized coverage loss removed, the next unknown is whether level 2 has a new correctness failure or merely heavier geometry. | Resume the D68 boundary with its explicit inherited-taint opt-in, keep every numerical setting fixed, use one bounded eight-worker observable wave, and stop after publishing level 1. | Confirmed complete. The dimension-11 `{6,14}` arm march and state publication finish at 246.00 seconds. Seven native observables contract in one wave through 582.03 seconds; export finishes at 649.76 seconds and publishes all twelve requested level-1 masters plus `/tmp/diffexp2-double-box-d70/checkpoints/double_box_planar_level1_boundary.mx`. Peak transient memory during singular bases is about 709 MB in the Wolfram report; contraction remains CPU-active and bounded. No new correctness error or retry occurs. Log: `/tmp/diffexp2-double-box-d70/level2.log`. |
| D-71 | Only the terminal scalar level remained unknown after D70. | Resume level 1 unchanged, retain full stage diagnostics, and run the repository's strict Laurent oracle rather than accepting the printed finite coefficient alone. | Transport completes in 61.75 seconds and numerically returns the expected sequence near `{12, 2.6260083756, -77.808155759, -201.73675882, -247.0036783}`. The oracle correctly rejects publication because the row also retains an ε^-5 centered-zero remnant of size about `3.7e-14`; the analytic double-box window must start at ε^-4. This exposes a missing example-specific final pole-floor contract, not a wrong finite value. Log: `/tmp/diffexp2-double-box-d71/level1-final.log`. |
| D-72 | D71's final-pole audit knows only the binary class “pole-free”; it cannot express the planar double box's proven ε^-4 floor, so generic relative trimming preserves a harmless ε^-5 cancellation remnant. | Generalize the final floor contract to example-specific minimum powers, add `double_box_planar -> -4`, audit every sub-floor ball against the existing absolute public tolerance, then project only that analytically forbidden prefix to structural zero. Add focused cases for the observed centered-zero remnant and a contradictory non-negligible ε^-5 pole. Replay only level 1 and require the strict Laurent oracle. | Confirmed. The focused native-dispatch suite passes 49/49 and still rejects the contradictory pole. The replay finishes in 57.16 seconds, publishes exactly ε^-4 through ε^0, and the independent oracle passes all five coefficients at eight digits; maximum scaled error is `1.300309033845827e-9`. The finite coefficient is `-247.0036783 - 0.e-8 i` with scaled reference error `3.2148271859615407e-10`. The run retains inherited diagnostic taint, so a clean full gate remains required for provenance/timing acceptance, but the complete numerical path and oracle now pass. Log: `/tmp/diffexp2-double-box-d72/level1-final.log`. |

Current progress: all six ladder levels complete and the strict eight-digit
Laurent oracle passes. A clean-from-scratch timing/provenance gate remains.
The cached FIRE preparation is retained. The acceptance run remains a clean
timed run after D-06 is fixed.

Disproved retries: increasing the level-5 Taylor order from 25 to 50 did not
reduce the D-04 reassociation enclosure; increasing the level-4 Taylor order
from 25 to 50 gained only one radius bit at the final paired line; matching-digit
escalation does not tighten that line and triggers the rejected transitive
14/16-digit cascade.

## Henn double-pentagon boundary

| ID | Observed issue | Controlled change | Result |
|---|---|---|---|
| H-01 | Reusing one fixed FT anchor at every level puts level 2 directly on the diagonal singular locus `x2=x1`. | Give the seven-level ladder distinct exact interior anchors `{1/5,3/10,2/5,1/2,3/5,7/10,4/5}` and include them in plans, caches, and checkpoints. | The repeated-anchor singular start is removed without changing the real `0..1` contours. Levels 7 through 4 complete rapidly in the component-1 reconstruction. |
| H-02 | Accumulated chart error was suspected after the long level-3 contour narrowly missed its public eight-digit matching gate. | Tighten every internal hop by two private digits while keeping the paper comparison at eight digits. | Rejected. The resulting `10^-10` gate fails earlier: Taylor order 50 reaches lower match 17 with midpoint ratio `1.35e-10`, while order 100 fails at lower match 14 with ratio `7.43e-9`. Both are already below the actual `10^-8` publication target. The private work guard was removed. Log: `/private/tmp/henn-ft-component1-workguard2.log`. |
| H-03 | Under the correct eight-digit gate, Taylor order 50 failed at upper match 22 with midpoint ratio `5.37e-8`; order 100 improved to upper match 23 and `3.04e-8`, but the retry controller stopped because both failures had 21 inconclusive coefficients. A later clean profile failed at the same upper match 20 while improving its ratio from `1.4356e-8` to `1.3912e-8` (3.1%). | Define bounded Taylor progress by the actual failure frontier: a later arm/match, fewer inconclusive coefficients, or at least a 1% reduction of the physical midpoint defect at the same handoff. | The false stall is covered by a runner fixture that must advance `50 -> 100 -> 200`; an earlier failure and sub-percent numerical drift are still rejected. |
| H-04 | Taylor retries were still a plausible explanation for the level-3 consistency floor. | Compare orders 50, 100, and 200 without changing the contour or digit contract. | Rejected. Order 200 does not tighten the failure and can make the midpoint defect worse. The issue is not an omitted spatial Taylor suffix, so another order doubling is unsupported. |
| H-05 | The fifth-radius overlap policy might have introduced too many charts or poor conditioning. | Run the same component with division order 6. | Rejected. The failure moves rather than disappears and the route is slower. Keep division order 5. |
| H-06 | Applying a global seven-digit internal certification cap appeared to fit the measured level-3 floor. | Apply that cap to every level while retaining eight-digit endpoint publication and paper comparison. | Rejected. Upstream producer levels legitimately need stronger internal contracts; a global cap creates a hard producer floor. Certification policy must be level-specific. |
| H-07 | Level 3 has a reproducible midpoint consistency floor near `1.64*10^-7`, independent of extra working precision and Taylor order, while its endpoints and the final paper value are separately certified. | Cap only level 3's chart-to-chart diagnostic at six digits. Keep eight-digit state propagation, endpoint publication, and the independent paper oracle unchanged. Split internal computation digits from the digits actually published to the downstream level so that the private cap cannot silently alter checkpoint requirements. | Confirmed architecturally and covered by the native-dispatch suite. Level 3 advances past the former consistency failure without weakening any final publication check. |
| H-08 | A terminal observable outside its publication radius triggered transitive producer-digit retries, including a level-6 sequence `14 -> 22` whose radius did not improve. | Record the first failing coefficient, midpoint, and radius exponent for each producer probe; retry only if the failure moves later or its enclosure tightens. | Confirmed. An unchanged or worse terminal probe now stops with `FeynmanTrickNativeMatchingProducerStalled` after one bounded attempt. The previous unbounded escalation is reproduced by a focused regression. |
| H-09 | The conservative `WP150/EO50` component-1 route clears level 3 but its first terminal producer probe spends more than 30 minutes in certified physical-disk construction. | Preserve all publication requirements and test the same route at `WP80/EO25`, allowing only the learned private epsilon reservoirs and bounded producer retries to change. | Partially confirmed. The lower-cost route clears all level-3 matches and reaches level 2. Its final paired line fails only at ε³ with radius `2^-19`; the midpoint agrees with the `WP150/EO50` run to about seven digits, so EO25 is not inventing a different branch. Raising the public level-3 producer target from eight to nine digits makes the radius worse (`2^-18`) and changes that high-order midpoint substantially. The new terminal-progress guard stops immediately instead of escalating again. Log: `/private/tmp/henn-ft-component1-wp80-eo25.log`. |
| H-10 | H-09's public producer retry raises every upstream publication target, so it changes the entire high-order boundary ladder and does not isolate whether level 3 merely needs one extra private computation digit. | Keep every publication target unchanged, retain the learned level-3 private halo, and raise only level 3's internal computation budget from eight to nine digits while its chart certification remains capped at six. | Rejected. With the known-good upstream profile restored, the ε³ midpoint and `2^-19` radius are bit-for-bit identical to H-09. Level-3 arithmetic is not the limiter; the width is already present in the boundary supplied by level 4. Log: `/private/tmp/henn-ft-component1-private-comp9-v2.log`. The preliminary `/private/tmp/henn-ft-component1-private-comp9.log` stopped at level 5 because it omitted already-required upstream publication extras and did not test this hypothesis. |
| H-11 | The generic producer retry responds to H-09 by raising level 3 and every upstream level with transitive safety guards. H-10 proves the consumer's private arithmetic is irrelevant, while the transitive probe changed the high-order midpoint and worsened its radius. | Raise only level 4's publication to its level-3 consumer from eight to nine digits, retaining the already accepted higher-level profile. Stop on any level-4 publication failure rather than inferring another upstream raise. | Confirmed the staged producer requirement. The existing level-4 boundary resumes, but level 4 cannot publish its new nine-digit boundary: the first failure is ε⁴ with a stable midpoint and `2^-24` combined radius, and it requests one additional computation digit. No expensive level-3 work is repeated. Log: `/private/tmp/henn-ft-component1-incremental-level4-publication9.log`. |
| H-12 | H-11 needs more arithmetic inside level 4, but its input boundary from level 5 already satisfies the accepted profile. Raising level 5 and every level above it would conflate private computation with input publication again. | Keep level 4's downstream publication at nine digits and its level-5 input unchanged; raise only level 4's internal computation target to ten digits while retaining its nine-digit chart-hop certification. | Rejected. The corrected 10/9/9 computation/certification/publication control leaves the ε⁴ midpoint and `2^-24` radius identical to H-11. The level-5 boundary is the limiter. Log: `/private/tmp/henn-ft-component1-level4-private-comp10-v2.log`. A preliminary run with an accidental 10/8/9 contract moved the failure earlier and is not evidence; log: `/private/tmp/henn-ft-component1-level4-private-comp10.log`. |
| H-13 | H-12 proves level-4 arithmetic cannot tighten its nine-digit output from the current eight-digit input boundary. | Raise only level 5's publication to level 4 from nine to ten digits, retaining level 5's already-established eleven-digit computation and all higher targets. If it publishes, replay level 4 and then level 3 without another profile change. | Level 5 and level 4 publish the tighter staged boundaries in 17 seconds. The optional direct-value-hop execution then spends more than 15 minutes inside one physical normalized-ODE disk certificate and is stopped; the same level-3 boundary resumed with basis-only matching finishes in about 5.3 minutes. Its ε³ midpoint changes consistently to `-23.4131310384 + 48.9918538925 I`, showing that the former eight-digit input was insufficient for this high order, but the combined radius remains `2^-19`. Logs: `/private/tmp/henn-ft-component1-incremental-level5-publication10.log` and `/private/tmp/henn-ft-component1-incremental-basis-only.log`. |
| H-14 | H-13 supplies a genuinely tighter level-3 input, but the final ε³ line remains just outside its eight-digit publication contract. It was not yet known whether the new input merely needed one more private level-3 computation digit. | Resume the H-13 level-3 boundary basis-only and raise only level 3's internal computation target from nine to ten digits; retain six-digit chart certification and eight-digit output publication. | Rejected. The ε³ midpoint and `2^-19` combined radius are unchanged, so level-3 arithmetic is not the limiter. Per-tile attribution shows the lower-arm width growing monotonically through its late ordinary tiles (approximately `2^-25` at tile 16 to `2^-20` at tile 22), rather than one isolated bad hop. Log: `/private/tmp/henn-ft-component1-staged-level3-private-comp10.log`. |
| H-15 | Each ordinary tile can factorize its own uncertain center value, but the arm aggregate still adds the resulting tile balls separately even though every later center is propagated from the same upstream boundary. This could repeatedly count correlated boundary uncertainty and appeared consistent with H-14's monotone late-tile growth. | Add typed factorization-eligibility diagnostics, rebuild the small upstream boundaries under the current source/profile, and replay level 3 before implementing a new whole-arm algorithm. | The proposed whole-arm change is not yet justified. The clean current-source rebuild completes levels 7 through 4 in 42.2 seconds, and level 3's eight-output native batch passes directly in 362.5 seconds without entering ordinary-row factorization. The former terminal ball failure is absent. The next blocker is a separate Wolfram Laurent-trim ambiguity after successful native export. Log: `/private/tmp/henn-ft-component1-factorization-eligibility.log`. |
| H-16 | After the successful level-3 native batch, strict public-prefix trimming aborts on an ε⁻³ remnant `-4.1991081584*10^-19` in level-2 master `{1,0,0,1,1,0,1,0,0,0,0}`. Its raw window is `[-3,3]`; the remnant lies inside the deliberate Laurent-leading ambiguity band relative to scale `54.30`. | Keep the library's strict zero classifier unchanged. At the FT boundary handoff, treat trimming only as an optimization: retain an ambiguous row as nonzero, log its master/window, and carry the one extra epsilon order. Catch and contextualize every other trim failure. | Confirmed. The checkpoint suite passes 46/46. Level 3 again completes its eight-output batch in 321.2 seconds; rows 1 and 7 conservatively retain their ambiguous ε⁻³ coefficients, all eight STEPWISE records publish, and a current level-2 boundary is written before the requested stop. Log: `/private/tmp/henn-ft-component1-conservative-trim-boundary2.log`. |
| H-17 | Resuming level 2 exposed the exact FIRE denominator `20-5d-45x+12dx`; after `d=4-2ε`, its simple pole approaches the lower endpoint. Expanding it as `1/(x-r(ε))` would consume one negative ε order per Taylor order, while rejecting every such pole prevents this example. | Audit the full exact connection. Its moving residue is a rank-one projector. Apply the certified gauge `G=I-R+(x-r)R` only when `R²=R`, both gauge inverse identities hold, and the complete transformed connection is exactly free of the candidate pole. Transform the full level boundary and every observable row as a dual pair, then apply the existing diagonal ε normalization. A counterexample with the same projector residue but a retained logarithmic coupling must remain rejected. | Confirmed structurally and in the real level. Solver tests pass 58/58, API tests 16/16, and the native preplanner tests pass 26/26. The corrected full-level plan adds one honest source order at level 2. A clean rebuild reaches level 2 in 341.6 seconds, installs the composite gauge, and advances beyond the former immediate E3. Log: `/private/tmp/henn-ft-component1-full-apparent-gauge-clean.log`. |
| H-18 | The first gauged level-2 march reaches the lower singular handoff but its Fuchsian frame is complete only through ε⁶ while the corrected request requires ε⁷. The typed result asks for exactly one additional private reservoir order; there is no residual mismatch or numerical blow-up. | Increase only level 2's private matching reservoir by one order and rebuild the upstream numerical boundary under the resulting exact plan. Keep working precision, Taylor order, public target, apparent gauge, and matching digits unchanged. | The one-order retry is still insufficient because the terminal physical residual has valuation `-2`; its common complete top remains ε⁶. This is a stacked window loss, not a repeated occurrence of the same one-order deficit. |
| H-19 | The retry controller reported only the next missing order, obscuring the combined loss through the terminal singular frame and the valuation-zero physical residual. | Compute the maximum common complete window after every valuation shift and expose it in the typed native failure; add a production-shaped C++ regression for a stacked terminal loss. | Confirmed. `valuation_zero_residual_complete_max` now reports the true deliverable prefix, so reservoir planning can cover the full stacked loss in one controlled retry. The focused matching suite passes. |
| H-20 | With the level-2 stacked loss covered, the next lower level had not yet been exercised under the corrected plan. | Run the revised four-level support-specific component-1 ladder and permit only typed private-reservoir learning. | Level 2 completes. Level 1 independently needs one additional private matching order; the learned exact profile becomes `{1,1,0,0}`. No public epsilon order or working precision is raised. |
| H-21 | A clean four-level run with the historical uniform `+1` prescription completes, but the normalized coefficients disagree with the paper: a spurious ε¹ term is `i Pi`, and the later imaginary parts lie on the wrong sheet. | Compare controlled sign flips without altering the differential systems, anchors, epsilon plan, or tolerances. | The numerical ladder is stable but the analytic-continuation contract is wrong. Flipping only level 1 removes the spurious leading term and produces the complex conjugate of the paper result. Flipping one global sign is not sufficient. Clean plus-sheet log: `/private/tmp/henn-ft-apparent-v5/henn-ft-run-1.log`. |
| H-22 | The runner used one scalar delta sign for both the deepest Symanzik boundary and every projected parameter contour. It also discarded the numeric unit when factoring `1-x`, losing the upper endpoint's physical orientation. Separately, zero-power lines were introduced as FT levels and removed later through singular endpoint limits. | Preserve oriented endpoint records `{x,s}` and `{1-x,s}`; partition scalar requests by actual positive-denominator support; evaluate a negative deepest `F` on an explicit upper or lower rim; and test the mathematically predicted X0 pattern: lower deepest rim with level signs `{+1,-1,-1,-1}`. | Confirmed. The support-specific ladder has four levels instead of seven. At WP80/EO25 it returns raw coefficients `-0.6168502750680795+0.5443965225759058 I`, `-1.359442646335472-3.400998248887559 I`, and `9.889390264586155-0.967625775770267 I`, agreeing with the paper-derived raw values to about 13--14 digits; the apparent ε⁻⁵ through ε⁻³ terms are centered numerical zero. Experimental proof log: `/private/tmp/henn-ft-support-predicted.log`. |
| H-23 | H-22 still used a hidden diagnostic level flip, and the paper oracle fed a retained `10^-112/eps` remnant to polynomial-only `CoefficientList`. | Add typed `"LevelDeltaPrescriptionSigns"` plan/environment/checkpoint/profile data, remove the diagnostic switch, rerun through the public manifest path, and make the oracle inspect Laurent coefficients directly while separately bounding every forbidden negative power. | Confirmed. The formal component-1 gate passes: errors at normalized ε⁰ through ε⁴ are `{6.10e-16,4.01e-15,1.20e-14,9.13e-14,3.51e-13}`, while the only forbidden normalized pole is `1.03e-112`. The first formal run learned only the exact private profile `{1,1,0,0}`. A clean-current-source rerun with cached FIRE preparation and that profile completes the numerical ladder in 113.73 seconds and the complete launcher/oracle path in 138.84 seconds. Initial-learning manifest and log: `/private/tmp/henn-ft-formal.oI7NL0/`. |
| H-24 (in progress) | The `--all` reconstruction correctly discovers one shared 109-master L0 basis and prepares all seven algebra levels, but the first level-4 native pass reaches lower hop 37 with a receiving frame complete only through ε²¹ when ε²² is required. The runner then rebuilds `RunFullIteration` because its preparation snapshot is attempted before the native preplanner hydrates the exact boundary reductions. | Accept the typed one-order request only at level 4, persist the learned profile, and hydrate every exact boundary batch before writing a fresh preparation snapshot. Keep WP80, EO25, the public ε target, and all matching digits unchanged. | The complete exact preparation is now cached. Profile `{0,0,0,1,0,0,0}` moves the failure frontier from hop 37 to hop 76, where a second independent one-order terminal-frame loss raises the learned profile to `{0,0,0,2,0,0,0}`. The automatic retry was stopped before repeating the expensive deterministic preprocessing. Live log: `.diagnostics/henn-double-pentagon-all-unbounded-resume.log`. |
| H-25 | Each level-4 retry spends roughly 2.5 hours after the epsilon basis is ready, even though no FIRE process is running. The exact batch contains 253 coefficient slots but only 61 unique expressions; every nonzero coefficient is already one polynomial numerator times one explicit denominator power, yet `CollectLevelIBPSingularFactors` applies `Together` and a second `Factor` to every slot. | Deduplicate coefficients, factor the already explicit numerator and denominator separately, subtract exact common-factor multiplicities, retain the old `Together` path only for additive/non-polynomial shapes, and memoize the result under the exact batch/payload identity. | The collector itself is fixed: on the saved level-4 batch it returns the ten exact factors in 0.26 seconds and a cache hit takes 0.04 seconds, with focused suites passing. However, the unrestricted profile-2 run still spends 12,797 seconds inside the subsequent level-4 native batch before successfully exporting all 23 rows and checkpointing level 3. The original collector diagnosis was therefore real but not sufficient to explain the complete level-4 runtime. Log: `.diagnostics/henn-double-pentagon-all-profile2-fast-factors.log`. |
| H-26 (in progress) | The successful H-25 level-4 handoff gives level 3 a source complete maximum of 22 and requires raw top 16. Every exact per-master capacity is at least 16, but the runtime ledger rejects it because it separately takes coefficient halo 6 from an endpoint-limit row and integration halo 1 from another row, then demands the nonexistent combined loss 7. After fixing the ledger, atlas preparation repeated the same cross-row maximum and raised `E6` with a fictitious required source top 23. | Compute each row's combined loss before taking a maximum. Extend native atlas preparation with one required target per integrand, so the integrate row contributes `17+5=22` and the limit row contributes `16+6=22`; never combine the target of one observable with the pole of another. Add both ledger-level and compiled native production-shaped regressions. | The epsilon preplanner passes 28/28, observable dispatch 63/63, checkpoint resume 5/5, and compiled native observable batch 31/31. The corrected contracts report per-row losses `{6,6}`, source requirement 22, integration halo 1, remaining coefficient halo 5, target top 17, and exact deliverable capacity 16. A real level-3 checkpoint resume is the remaining production confirmation; it is explicitly stale-accepted because these changes affect only downstream halo planning, not the saved level-3 numerical boundary. |
| H-27 | The H-26 resume remained at one full CPU core for more than 18 hours without advancing beyond the level-3 epsilon-basis line. Root discovery, segmentation, and one anchor chart took only `0.069`, `12.63`, and `21.5` seconds respectively, excluding atlas geometry as the cause. A bounded exact-input reproducer then stalled between affine-pair construction and coefficient extraction with a GMP polynomial-GCD stack. | Reconstruct the exact normalized level-3 matrix and boundary from the checkpoint, verify its matrix hash, and profile every owner-preparation phase. Replace the per-`{lag,row,column}` `Coefficient` plus `Together`/`Cancel` loop with one exact `CoefficientList` extraction per already-cleared polynomial entry. Preserve the old path for non-polynomial input and require exact cleared-equation parity. | Confirmed at the production anchor. The exact input hash matches; the new one-pass extraction takes `121.45` seconds, pole-depth analysis `7.12` seconds, and the complete native regular anchor finishes successfully in `338.26` seconds. The obsolete production kernel was stopped after 18h24m because it was still in the replaced phase. Exact affine-hoist regressions pass 8/8. Full level-3 ladder confirmation is now restarting from the unchanged saved boundary with stage timing. |
| H-28 | H-27 makes one chart finite, but the level-3 atlas contains 91 lower/upper charts and the profiled run still spends about 70 seconds preparing each ordinary equation owner. Every owner expands the same globally cleared 23-by-23 polynomial after substituting a different exact affine center. | Store the exact global numerator/denominator coefficient lists once. For exact numeric chart geometry, derive each local list by the triangular binomial translation formula instead of substituting and expanding full expressions; retain the expression/GCD fallback for symbolic geometry. Preserve exact common-center valuation cancellation and compare against the legacy cleared equation. | Confirmed. The production-hash anchor's affine extraction falls from about 184 seconds under reduced priority to `1.87` seconds; the complete valid anchor falls from `338.26` to `233.81` seconds. Solver 58/58, physical payload 10/10, regular owner 8/8, and affine parity 8/8 pass. A follow-up attempt to pre-numericize cancellation-heavy algebraic physical-payload coefficients is rejected: it removes `N::meprec` warnings but regresses the same anchor to `238.10` seconds, so it was removed without restarting. The active profiled ladder uses the global-coefficient translation. |
| H-29 | The first level-3 singular SCC owner remained at one full core for `2692.7` seconds after its last affine-hoist phase. The optional exact singular-tail classifier materialized dense 23-by-23 block-diagonal gauges, divided the transformed equation back into a rational matrix, and then recomputed a global denominator/GCD only to discover that this endpoint has no admissible formal-unit tail frame. | Retain the parent's already-cleared exact `q(t,eps)/C(t,eps)` equation, apply the certified SCC gauge one target/source block at a time, normalize only the exact common center monomial, and encode the resulting polynomial pair directly. A remaining lower center valuation now proves a genuine pole immediately; rational-in-`t` gauges and nonunit moving factors retain the established generic fallback. Normalize Mathematica's empty `CoefficientList[0,x]` to the typed zero list while exercising the same path. | Confirmed on the exact production matrix/hash. All 169 SCC block pairs canonicalize, and the production-shaped classifier reports the same unavailable optional tail with the explicit reason `Fuchsian block gauge retains a genuine center pole` in `39.28` seconds under concurrent load (about 69x faster than the live old-code owner). The solver, affine-hoist, physical-payload, SCC manifest, CASE-P, polar-shadow, singular-block, and block-column regressions pass. Logs: `.diagnostics/henn-h29-tail-diagnostic.log` and `.diagnostics/henn-h29-tail-fast-classifier.log`. |
| H-30 | H-29 removed the polynomial-gauge rebuild, but the final level-3 endpoint at center `1` has a rational-in-`t` block gauge. The retained compatibility path again divided `C` by `q` and rebuilt a whole 23-by-23 rational matrix; it stayed at one full core for more than 70 minutes after block reduction and reached an approximately 19 GB physical footprint. | Clear only the canceled denominators introduced into the retained `q/C` pair by the small SCC gauges. Compute the common center valuation directly from each canceled numerator/denominator and the one gauge denominator. If `q` has strictly higher center valuation than an active `C` entry, publish the rigorous genuine-pole decision before multiplying or dividing the full matrix; retain the legacy clearer only for a nonunit moving common factor. | Confirmed on the exact production matrix/hash. The center-`1` probe returns the same unavailable optional tail with reason `Fuchsian rational block gauge retains a genuine center pole` in `121.74` seconds cold, including `56.95` seconds reconstructing the uncached parent and `46.85` seconds reducing all block gauges. The new denominator/valuation proof itself takes `17.88` seconds (`6.66` extraction, `0.31` LCM, `0.40` content, `10.51` valuation) and never materializes the polynomial matrix. Physical-owner tests pass 15/15, solver tests 58/58, affine-hoist tests 9/9, and the focused singular SCC tests pass. Log: `.diagnostics/henn-h30-endpoint-tail-early-valuation.log`. |
| H-31 | The corrected all-component run prepared all 89 level-3 receiving owners and production-confirmed H-30 at center `1` in `4.15` seconds after block reduction. During the subsequent streamed march, however, Wolfram memory grew by roughly 130--150 MB after every regular basis. The final lower SCC basis drove swap above 42 GB and macOS killed the kernel with status 137 after 24 certified lower hops. | A consumed basis no longer leaves its complete serialized recurrence operator in `$cppStaticOperatorCache`. Drop that cache immediately after the native hop takes ownership of the opaque basis locals, while deliberately retaining the original-system clearing registry, global cleared equation, SCC provenance, and all native handles needed by later singular charts. Add both one-drop-per-hop and cache-scope regressions. | Rejected as sufficient, retained as a valid narrow cleanup. The CASE-P terminal fixture rejected a broad preparation-cache clear because it removed the original-system registry. The narrow drop passes observable transport 32/32, CASE-P lifecycle 4/4, checkpoint resume 6/6, and the FT facade 26/26. In production H31 it released `48.4`, `27.0`, and `27.6` MB after the first three lower hops, but post-hop memory was still `8.110`, `8.226`, and `8.333` GB: the remaining linear growth is not that memo. H31 was stopped deliberately before hop 4. Logs: `.diagnostics/henn-double-pentagon-all-h30-gauge-local-tail.log` and `.diagnostics/henn-double-pentagon-all-h31-bounded-stream.log`. |
| H-32 | H31 proves that the main approximately 107 MB/hop growth survives after the completed operator memo is cleared. `nativeStreamTransportArm` appends every coefficient-bearing materialized local to `tiles`; observable contraction begins only after the complete lower and upper marches have published states. Thus “streaming” bounds receiving-basis construction but still retains one full source slab per tile, and the final singular basis is asked to allocate at the arm-wide memory peak. | Fuse stored-tail observable contraction into the march. For each exact tile, prepare and consume its rows while that tile is the current local, add only its certified epsilon vector and compact provenance to native accumulators, then release the coefficient slab after the next hop seals its lineage. Retain only the current/terminal local, exact tile-plan owner, compact hop certificates, and accumulator state. Endpoint observables consume the terminal local directly and published rolling lines/endpoints strongly own only the minimal immutable closure required for export and checkpoint restore. | Implemented across the C++ retained-object protocol and Wolfram dispatcher. A production-shaped mixed deferred batch performs a real hop, a rolling integral, and a direct endpoint with zero published transport states; line and endpoint export plus state-free native checkpoint save/restore pass. The low-level rolling fixture proves exact paired value `5/6`, endpoint value `1`, public local release, and identical post-restore exports. The clean native rebuild passes all 50 targets; focused pair and endpoint executables pass, and the Wolfram observable-batch, streamed-pair bridge, endpoint bridge, and CASE-P checkpoint suites pass 33/33, 4/4, 6/6, and 4/4. The first production replay was stopped before completing hop 1 because no rolling events appeared; H-33 identifies the dispatch mismatch. |
| H-33 | The H32 production replay reproduced H31 through all 89 receiving owners, including the 465.1-second algebraic owner versus H31's 461.8 seconds and the center-1 rational-gauge proof. At observable dispatch it entered the old state-retaining march: the rolling path required contraction chunk size exactly 1, but the generic runner intentionally defaults chunk size to its two contraction threads. The rolling implementation already owns one independent stream per observable and has no mathematical or ownership dependence on the later batch chunk size. | Remove the false chunk-size eligibility guard. Keep each rolling stream's effective publication chunk size at one, while accepting any validated configured legacy chunk size. Extend the real deferred mixed lifecycle fixture to two stored integrals, a direct endpoint, and configured chunk size 2; require two rolling lines, zero transport states, ordered export, and state-free checkpoint restore. | The expanded production-shaped suite passes 33/33. Production H33 selected `rolling-stream-begin observables=23`, contracted tiles before each hop, and released the public previous-local token, but its first five post-hop memory readings were still `8.076`, `8.193`, `8.299`, `8.406`, and `8.512` GB--essentially H31's old slope. The run was stopped at lower hop 6. Dispatch is fixed; rolling publication alone is not sufficient. Log: `.diagnostics/henn-double-pentagon-all-h33-rolling-chunk2.log`. |
| H-34 | The only deliberately cumulative objects introduced by H32 are each observable stream's per-tile row provenance and coefficient-wise tile conditioning. Henn rows currently use complete canonical expression JSON as `exact_identity`; retaining that JSON once per tile and per observable can replace the coefficient slabs with a comparably large provenance staircase. H33 did not report retained object byte classes, so this hypothesis had to be measured rather than inferred from `MemoryInUse` alone. | Bind prepared rational rows and their active multipliers by fixed-size SHA-256 digests of the existing canonical exact identities. For rolling streams retain compact per-tile conditioning summaries while preserving full coefficient-wise conditioning for the completed arm/pair. Report cumulative retained row-record and tile-diagnostic serialized bytes on every rolling add. | Rejected as the source of the approximately 107 MB/hop growth; retained as a valid compact-provenance improvement. The focused Rational-row, rolling-pair, endpoint, observable-batch, and CASE-P gates pass. In production every per-observable row record is only about `0.4`--`3.5` KB/tile and each diagnostic about `0.2` KB/tile, yet the first three post-hop readings are `8.0760`, `8.1925`, and `8.2992` GB, indistinguishable from H33. H34 was stopped before hop 4. Log: `.diagnostics/henn-double-pentagon-all-h34-compact-rolling.log`. |
| H-35 | H34 proves that the linear retention is neither published coefficient slabs nor rolling row/conditioning metadata. The remaining candidates are native session owners/locals and Wolfram-side per-hop objects that survive the nominal local release and static-operator cache drop. | Report fixed-size persistent-session counters and compact Wolfram object byte counts immediately after each hop. Drop the completed basis's chart-local cleared equation, legacy local clear, and static operator while preserving the original-system/global clearing registries, physical q/C cache, native SCC cache, and all native handles. | Rejected as sufficient, retained as a valid peak-memory cleanup. The production-hash cache probe finds that the first three ordinary cleared-chart records grow `$chartClearedCache` to `315.6`, `626.4`, and `937.1` MB; the scoped drop returns it to 128 bytes. In the full H35 replay the first hop also releases 1.984 GB of accumulated chart/legacy/static memo state, but post-hop memory still rises `6.1518`, `6.2683`, `6.3750`, and `6.4821` GB through lower hops 1--4. Visible native counts remain exactly two locals, zero matches, and 23 streams, while the Wolfram response/current/stream records remain below 101 KB, 2 KB, and 85 KB respectively. H35 was stopped at hop 5. Logs: `.diagnostics/henn-h35-consumed-cache-probe.log` and `.diagnostics/henn-double-pentagon-all-h35-consumed-basis-cache.log`. |
| H-36 | H35 leaves an exact approximately 107 MB/hop slope despite bounded live locals and small Wolfram transport records. `session.counters` had not reported prepared recurrence charts or the complete static-payload certificates held by CppBackend's prepared-token registry. | Add process-wide retained-local lifetime/coefficient counters plus visible shared-owner counts. Give every monolithic regular basis its collision-certified prepared token, and after a consuming hop erase that token so CppBackend releases both the transient C++ recurrence chart and its full static-payload certificate. Exercise the exact production level-3 matrix, boundary transform, epsilon window, and first three regular lower hops in a bounded reproducer. | Root cause confirmed and the focused fix is successful. Before the fix, hops 1--3 grow native `charts` from 2 to 4 and post-drop memory from `527.80` to `634.49` to `740.64` MB while nonterminal live locals return to two. After the fix, all three hops retain exactly one anchor chart and memory is `423.52`, `424.15`, and `424.77` MB: only about 1.25 MB total growth instead of 212.8 MB. The full replay prepared all 89 level-3 receiving owners, then lower hops 1--24 remained nearly flat at `6.0475` through `6.0619` GB. It died with status 137 during the final singular lower hop after Wolfram Engine reported a license error while another Engine-14 kernel was active; this is not a recurrence mismatch, but the retained upper atlas still left too little peak-memory headroom. Focused regular-basis, observable-lifecycle, and regular-owner suites pass 4/4, 34/34, and 9/9. Logs: `.diagnostics/henn-h36-local-lifetime-probe.log`, `.diagnostics/henn-h36-local-lifetime-release-probe.log`, and `.diagnostics/henn-double-pentagon-all-h36-transient-chart-release.log`. |
| H-37 | H36 proves that completed transient recurrence charts no longer accumulate, but a deferred atlas still retains 89 complete transformed `ChartSystems` and 82 duplicated regular-owner `PhysicalPreparation` payloads while marching either arm. This keeps about 6 GB live through all ordinary lower hops and forces the terminal singular basis to allocate at the atlas-wide peak. | During atlas construction, capture compact immutable row recipes, remove every retained transformed chart and duplicated physical-preparation record, and retain the original source system exactly once. During the march, reconstruct exactly one receiving chart from its certified descriptor immediately before its hop, then release it. Drop atlas-wide consumed chart caches before marching while preserving the global clearing, physical, SCC, and native owner registries. Use Mathematica 15 for the full replay so it does not contend for the active Engine-14 license. | The compact Wolfram atlas is successful but insufficient as a complete peak-memory fix. Its focused probe is only `1,707,960` bytes and three exact lower hops remain near `420` MB. In the full replay all 24 ordinary lower hops pass with post-hop memory rising only from `6.0688` to `6.0832` GB, proving the former approximately 107 MB/hop leak is gone. The final singular lower basis then runs for about ten minutes and is killed by signal 9 with no Mathematica or structured C++ failure. C++ `StoredTilePlan` still strongly owns all 89 receiving owners, and `StoredPlannedMatchHop` retains that plan, so dropping the Wolfram chart records cannot remove the approximately 6 GB native baseline. Logs: `.diagnostics/henn-h37-compact-atlas-probe.log` and `.diagnostics/henn-double-pentagon-all-h37-compact-atlas.log`. |
| H-38 | H37 localizes the remaining failure to the terminal singular receiving-basis allocation, but the full replay cannot distinguish the basis's intrinsic peak from its interaction with the retained 89-owner native plan. | Reconstruct only the exact production level-3 terminal lower chart and prepare its SCC owner, then execute `nativeReceivingBasis` without building the full transport plan. Keep the production matrix, gauge, epsilon window, Taylor order, precision, and eight-thread backend unchanged. | Invalid control, stopped cheaply. The first script omitted the runner's complete dimension-symbol normalization for FIRE singular factors, producing 19 charts instead of the production 26 and an algebraic-scale terminal chart. Owner preparation used only about 610 MB, but basis execution correctly rejected a non-rational coefficient after 9.1 seconds, before the allocation under investigation. This is a reproducer defect, not solver evidence. Log: `.diagnostics/henn-h38-singular-basis-alone.log`. |
| H-39 | H38 did not reproduce the production singularity atlas. | Apply the exact runner normalization to all FIRE singular factors, retain only factors depending on the physical parameter, verify the atlas has 33 roots and the lower plan has 26 charts, and only then repeat the isolated terminal basis. | The normalized atlas has the correct 33 roots but still only 19 lower charts. The run was stopped during owner preparation. Comparison with the production setup shows the missing input is the level-specific branch prescription: it changes the projected clearance geometry even though it does not change the number of singular roots. Log: `.diagnostics/henn-h39-singular-basis-alone.log`. |
| H-40 | H39 omitted the production level-3 delta prescriptions, so its chart geometry was not equivalent to H37. | Reproduce `levelDeltaPrescriptions` exactly with the level-3 sign `-1`, load the same calculation and matching configuration as the runner, require 33 roots and 26 lower charts, and repeat the isolated basis only after those gates pass. | The endpoint prescription is now present and the atlas still has 33 roots, but the lower plan remains at 19 charts. The run was stopped before completing owner preparation. Branch prescriptions were a real omitted input but do not explain the geometry difference. Log: `.diagnostics/henn-h40-singular-basis-alone.log`. |
| H-41 | The hand-built reproducer still differs from the production lower plan despite matching the matrix hash, root count, and endpoint prescription. | Run the real public all-component launcher with `FT_DUMP_NATIVE_PLANS=1`, using the cached preparation and level-3 boundary, and stop before native atlas preparation. Capture the exact production terminal descriptor rather than continuing to infer it. | The public launcher stops as requested before atlas preparation and confirms 33 roots plus plans `{26,65}`. Its raw terminal lower descriptor is exactly the one in H38--H40: center `0`, incoming point `1596889/23446746`, and scale `(820-10 Sqrt[1789])/987`. Thus the extra preceding charts do not affect the isolated singular basis. Log: `.diagnostics/henn-h41-production-plan-dump.log`. |
| H-42 | H41 proves the raw terminal descriptor matches, but the production atlas applies `bridgeNativeRegularPlanScales` before preparing charts; the first isolated probe omitted that certified rational containment bridge and therefore selected an inapplicable exact-parity path on an algebraic coefficient. | Apply the same bridge to the isolated lower plan before `PrepareChart`, verify the bridged terminal descriptor, and repeat the cached SCC-owner plus singular-basis construction. | The exact singular calculation clears its computational peak: owner preparation is about 543 MB, the eight-column phase peaks near 3.9 GB, and memory subsequently falls below 0.5 GB. After every column has completed, a native stack sample finds the main thread entirely inside `boost::json::serialize`, repeatedly growing and copying the response string; it remains there for more than five minutes while RSS and virtual size fluctuate. The run is deliberately killed at 14.9 minutes. This proves both that the singular mathematics fits without the 89-owner plan and that the supposedly opaque SCC batch still duplicates enormous owner-bound residual identities in every column response. Log: `.diagnostics/henn-h42-singular-basis-alone.log`; sample: `/tmp/h42-sample.txt`. |
| H-43 | `scc.solve_columns` calls the full local `summary()` for every retained column. Each summary repeats exact operator, source, owner-signature, physical-payload, and residual-provenance strings even though the local and SCC remain their native authorities. The final contiguous JSON serialization creates the fatal extra peak. | Add a bounded owner-bound-residual reference containing stable fingerprints and byte counts, use it only for SCC batch summaries, and retain the full exact binding in the native local/checkpoint and single-column API. Add a batch regression proving the exact binding is omitted while the authority reference is complete. | Partially confirmed. The isolated production singular basis now returns successfully: native execution takes `351.49` seconds and the end-to-end Wolfram call takes `903.57` seconds. Thus the singular mathematics completes in 5.86 minutes, but another 9.20 minutes remain in response transfer/parsing. The process peaks near 7 GB during the parallel solve and exits normally. Log: `.diagnostics/henn-h43-singular-basis-compact-response.log`. |
| H-44 | H43 proves the residual identity was not the only unbounded field. The generic single-local summary also publishes complete tail-majorant/operator metadata; redacting one field after constructing `summary()` leaves the batch wire contract vulnerable to every future self-describing payload. | Construct the SCC batch-local response directly from its bounded opaque-handle contract: dimensions/windows, checkpoint and compact source/column/residual references, plus timing and pseudo diagnostics. Do not call the full single-local `summary()` at all. Add negative assertions for full tail metadata, local metadata, and source-operator identity. | The exact Rational batch now returns through the bounded contract, but the run remains in large Wolfram-side copies at 12 minutes and is stopped because its already-loaded library cannot test the next fix. Source inspection identifies a second instance of the same bug: every sequential Rational-to-Acb column specialization returns `imported->summary()` with the full exact owner and tail records. Log: `.diagnostics/henn-h44-singular-basis-bounded-response.log`; sample: `/tmp/h44-sample2.txt`. |
| H-45 | The proactive CASE-P route first solves the complete Rational basis, then specializes each retained column into the paired Acb SCC. The specialization API is another opaque-handle exchange but returns the full local summary once per column. | Reuse one explicitly named bounded opaque-local reference contract for both `scc.solve_columns` and `local.specialize_rational_shadow`. Keep the complete self-describing summary available for explicit single-local inspection, residual certification, and checkpoints. Add a specialization regression that rejects full tail metadata, local metadata, and source-operator identity on the wire. | Confirmed. Seven focused SCC, specialization-lifecycle, and transport tests pass. The isolated production singular basis completes end to end in `419.68` seconds (6m59.7s), including exact Rational execution in `272.60` seconds plus all Rational-to-Acb specializations. It closes at about 359 MB Wolfram memory. Compared with H43's `903.57` seconds, bounding both opaque response seams removes about 8.1 minutes and satisfies the seven-minute singular-basis gate. Log: `.diagnostics/henn-h45-singular-basis-all-bounded.log`. |

## Generic Feynman-trick examples

| ID | Observed issue | Controlled change | Result |
|---|---|---|---|
| FT-01 | The documented shell examples invoked the private `run_ft_stepwise2.m` implementation directly, so launch configuration, validation, and output handling were duplicated. | Add one public `RunExample.m` registry which builds `FeynmanTrick\`PipelinePlan` values and executes `FeynmanTrick\`RunIntegrationPipeline`; reduce all six shell files to thin launchers. | `--check` validates all six plans. Bubble, sunrise, box-bubble, and kite execute through the public facade. Banana4 is plan-validated only, as requested. |
| FT-02 | Kite repeatedly requested more private epsilon reservoir although its exact local honestly contained lower Laurent rows that were discarded when the evaluation frame was narrowed to the incoming request. | Preserve every honest lower local row while keeping the requested upper edge unchanged, and add a production-shaped lower-Laurent terminal match fixture. | The focused match test passes and the false reservoir loop is gone. |
| FT-03 | After FT-02, successive kite matches exposed independent producer-digit and same-arm private-clearance deficits. Linear one-order retries revisited equivalent failures. | Separate consumer matching halos, producer delivery losses, and producer matching-digit extras; grow a repeated same-arm matching halo exponentially. | Kite completes through the generic launcher with finite value `0.22398391910744253038193`, no retained poles, private halos `{1,0,42,0}`, and producer digit extras `{0,2,4,6}`. |
| FT-04 | Unequal banana levels 3 and 2 pass, but level-1 upper match 29 assigns all 42 required residual coefficients unbounded balls. Their zero-radius midpoint residual is about `1.0e-68`, and each singular Frobenius tail is only about `1e-54`--`1e-61`. | Compact non-finite JSON diagnostics and inspect the terminal singular-to-ordinary bridge instead of raising epsilon depth or working precision. | The ordinary disk-wide Gronwall tail, not GCD or the singular tail, is approximately `2^3,730,000`. The automatic Taylor `70 -> 140` retry was stopped as misclassified. Log: `/tmp/diffexp2-unequal-banana-generic.log`. |
| FT-05 | The first hypothesis was that returning the first certified ordinary witness selected a weak disk. | Compare witness radii in a scalar bridge fixture and reverse the candidate order only under test. | Disproved. Under the Gronwall theorem the midpoint disk is tighter than the outward disk. The production order was restored; do not repeat this radius-order-only change. |
| FT-06 | The exact q/C recurrence retains cancellations that the global Gronwall norm destroys. A new all-future ordinary recurrence certificate works in controlled bridges but initially fell back in the real unequal banana. | Reuse the exact Rational all-future transfer theorem with the propagated Acb induction prefix; log fallback reasons; test epsilon weights through `2^20`. | The theorem is rigorous and exposed an inconsistent frame-change test equation that the old huge error ball had hidden. In the real bridge the midpoint recurrence contraction is `rho=1.657362`, independent of additional epsilon weight, so weight expansion alone is disproved. Logs: `/tmp/diffexp2-unequal-banana-recurrence-tail.log` and `/tmp/diffexp2-unequal-banana-recurrence-weighted.log`. |
| FT-07 | A midpoint ordinary witness is too large for the recurrence theorem even at Taylor order 800, while the target lies well inside a smaller admissible disk. | Search recurrence witnesses beginning at `R=9|delta|/8`, disable Gronwall short-circuiting during that search, and add a fixture where a loose radius has `rho>1` but a tighter radius certifies. | Confirmed. The focused C++ tests pass and a clean generic-launcher run completes every unequal-banana level. Level 1 clears the former chart-29 failure in 989.17 seconds and publishes finite value `5.83402729266214946741`; the pipeline exits successfully. Log: `/tmp/diffexp2-unequal-banana-tight-recurrence.log`. |

Current status: bubble, sunrise, box-bubble, and kite pass through the generic
facade. Unequal banana also passes end to end through the generic facade.
Banana4 remains intentionally unexecuted.

## Four-loop banana

This table is the historical recovery summary through B4-145. The
investigation subsequently advanced through B4-248 and is now intentionally
paused. [`Banana4Status.md`](Banana4Status.md) is the canonical current
handoff; [`banana4-debug-log.md`](banana4-debug-log.md) contains the complete
experiment ledger.

| ID | Observed issue | Controlled change | Result |
|---|---|---|---|
| B-01 | Earlier runs failed in upper terminal matching and endpoint geometry before producing a lower-level boundary. | Retained exact-right/factorized terminal work, geometry corrections, and the matching fixes recorded in their commits were exercised together in the latest production-profile run. | The closest run completed levels 4 and 3, wrote `banana4_level2_boundary.mx`, and entered level 2. No final banana4 value has been produced. |
| B-02 | Level 2 failed in `terminal-factorized-adjoint-line-contraction:lower:5`: the adjoint forward residual was inconclusive from epsilon powers 8 onward although its finite window extended through the required power 25. | Diagnosis only. Preserve the level-2 boundary checkpoint for a focused reproducer; do not claim completion and do not substitute a checkpoint resume for the eventual clean acceptance run. | Historical blocker, superseded by the controlled B4-79--B4-89 campaign in `Docs/banana4-debug-log.md`. Do not retry this old configuration. |
| B-03 | The later production chain successively exposed lost exact physical-equation provenance, an uncancelled hoisted center factor, adjoint orientation/private-reservoir/tail-attribution defects, loss of exact-shadow correlation, numeric-ball equality used as chart identity, and finally an overlap-recenter frame-width mismatch. | Fix each contract at its ownership boundary and add a production-shaped regression before advancing. The detailed evidence and disproven configurations are recorded in `Docs/banana4-debug-log.md`. | The WP300/EO50 level-3-to-level-2 chain passed singular matching, state publication, and composed-adjoint contraction; a shortened run completed four of six observables. The subsequent pinned WP500/match-25 gate exposed the overlap-recenter mismatch, which is now fixed and covered; the native suite passes 48/48. No final banana4 value has yet passed the Bessel oracle. |
| B-04 | The first pinned WP500/EO50/match-25 replay after the overlap repair reached its last observable and exposed two terminal rows outside the ordinary composed-adjoint theorem. | Select the independently certified physical-coordinate contraction only for the typed Laurent/log-center line and nonunit-center endpoint cases; retain the authoritative composed route everywhere it applies. | B4-127 passed the clean level-2 gate: all six paired observables completed, seven exports were produced, the level-2 boundary was checkpointed, and the full batch finished in 388.6 seconds under the 420-second ceiling. |
| B-05 | The clean final gate used 15 digits uniformly. Its coarser upper geometry exhausted certified value transport at seven regular centers and the first level-3 paired result missed publication by one digit at ε^-2. | Preserve the 15-digit final consumer, but seed its immediate level-2 producer at 16 digits and apply the existing two-digit guarded producer ladder, giving levels 2/3/4 digits 16/18/20. Retain the bounded typed retry driver for later levels. | B4-128 is a recorded failure, not a retry target. B4-129 tested this guarded ladder and advanced to a later one-digit deficit; it is also closed. No final result has yet passed the Bessel oracle. |
| B-06 | The 16/18/20 profile cleared the first level-3 observable but the fourth missed private ε^13 by one digit, and the driver proposed 16/19/21. | Do not scan one digit at a time. Reuse the real passing profiles already in the detailed ledger: level 2 at 19 digits (B4-101/B4-102) and level 3 at 25 digits (B4-127). | B4-130 tested `<|2->19,3->25,4->25|>` under the unchanged 15-digit final/public contract; its result is recorded in B-07. |
| B-07 | B4-130's 19/25/25 producer profile cleared the earlier level-3 deficits, but level 3 took 1343.83 seconds and used bases at all eleven ordinary upper charts. Level 2 then missed a private ε^25 line coefficient by one digit and requested level-2 accuracy 20. | Stop the late automatic rebuild. Compare B4-130 with the fast B4-127 contract before raising digits again: identical geometry and epsilon ledger were certified internally at 21 versus 25 digits because the current rule caps internal certification at downstream publication plus two. | B4-130 is closed and its full log/checkpoints are preserved. The next action is a focused certification-contract audit from its level-3 input checkpoint, not another full run or a one-digit scan. |
| B-08 | B4-131 restored full 25-digit internal certification while resuming B4-130's unchanged level-3 input, but the first four upper hops reproduced the same Taylor-800 exhaustion and basis fallbacks within 46.5 seconds. | Stop immediately and revert the experimental policy default. The certification cap is not the regular-hop cause; isolate the physical value/tail changes introduced after B4-127, especially the large `04eee40` C++ patch. | The failed hypothesis cost under one minute and is closed. No level was rebuilt and no final result was claimed. |
| B-09 | The level-3 boundary itself is already degraded: its second and third coefficients retain only about 32 and 31 digits, while the corresponding old passing checkpoint retains about 73 and 72. | Disable `04eee40`'s automatic factorized incoming-value evaluation, resume the same level-3 input, and stop after four ordinary hops. | Disproved in B4-132. The decisive Taylor-800/basis-fallback signature is unchanged. The diagnostic switch was removed; do not repeat it. |
| B-10 | `04eee40` also lets an overlapping factorized stored-row integral replace the direct integral without requiring a tighter enclosure. | Disable only that row replacement, rerun fresh level 4, and compare its level-3 boundary coefficientwise with B4-130 and the old passing checkpoint. | Disproved in B4-133. The second coefficient remains on the degraded midpoint with only 31.76 digits of combined accuracy, and the third has 30.70; the old checkpoint has 73.45 and 71.95. The diagnostic guard was removed and the production library rebuilt. The loss is upstream of, or common to, both row-integration routes. |
| B-11 | The exact introducing commit was unknown because the old passing artifact predated several aggregate commits. | Use the 11--21 second level-4 boundary coefficient-accuracy gate across source history instead of another full chain. | B4-134 identifies `0e9a508` as the first bad commit: parent `4ac2f44` has 73.45/71.95 digits, while `0e9a508` has 31.76/30.70. Its new physical q/C tail replay replaces the parent's full-basis upper hops. The nominal `DE2_VALUE_TRANSPORT=0` control does not disable this native path and is closed. |
| B-12 | The shortcut currently certifies only the local match-digit target, but its result is a producer for an ill-conditioned singular reconstruction. | Before changing policy, use the existing producer-digit mechanism at level 4: computation 70, level-3 consumer 25, and stop immediately after the level-3 boundary. | Confirmed in B4-135. The physical shortcut remains active, raises only its private Taylor certificates to mostly 252/378, and publishes a boundary with 73.45/71.95 certified digits in 52.97 seconds. Publication remains 25. The old basis midpoint differs after the public contract and was never spatially certified; do not restore it merely because its ball was narrow. |
| B-13 | Determine whether the restored upstream enclosure removes the level-3 fallback cascade rather than merely looking narrow in isolation. | Resume the B4-135 checkpoint with level 3 at 25 digits, stop after boundary 2, and enforce the existing 420-second gate. | Transport confirmed in B4-136: all eleven ordinary upper hops use certified physical evolution and state publication finishes at 73.91 seconds. The run then times out during the first of three two-observable contraction waves. This is a separate scheduling regression; no arithmetic failure occurs and no boundary is published. |
| B-14 | The current two-thread/two-entry contraction default serializes six expensive banana observables, whereas the old passing gate contracted the bounded batch concurrently. | Replay the exact B4-136 input with six contraction threads and one six-entry chunk; change no numerical setting. | B4-137 removes the three-wave serialization and sustains about 5.6 cores, but still times out at 420 seconds after state publication at 74.90 seconds. All visible lower rows certify and no arithmetic failure occurs. Do not rerun or merely raise the ceiling; instrument lower/upper/cancellation/publication phases per observable and localize the remaining contraction regression. |
| B-15 | Phase timing showed each ordinary stored-row direct integral cost milliseconds, while unconditional factorized impulse integration cost 23--36 seconds per tile; replaying q/C itself cost only about 0.25 seconds. | Publish rigorous direct paired lines first and invoke ordinary-row factorization only after a typed final publication failure. Preserve terminal contraction, overlap checks, and honest counters. | Focused C++ tests pass. Five of six level-3 observables immediately avoided factorization; the sixth exposed that the publication gate was checking private halo coefficients, not a need for unconditional factorization. |
| B-16 | The retained level-3 output extends through private ε^40, but level 2's independently planned public edge is ε^6. The dispatcher set observable `RequiredCompleteMax=40`, so private ε^29/ε^33 deficits repeatedly rejected an already sufficient public boundary. | Keep observable `Max=40` for later arithmetic but set `RequiredCompleteMax=6` from `PublicDeliverableCompleteMax`. Add a fixture retaining through ε^7 while certifying only ε^3, then regenerate levels 4 and 3 with clean provenance. | Confirmed. The dispatch suite passes 51/51. Clean B4-145 completes level 4 at 17.75 seconds and level 3 at 188.71 seconds, exports all seven level-3 results, and writes a current-source level-2 boundary without any ordinary factorization retry. Full log: `/private/tmp/diffexp2-banana4-b4-145-public-prefix-clean.log`. |

Current progress: the old B-02 and B4-145 blockers are no longer active.
B4-241 produced a current-source level-1 boundary, B4-245 reached the
authoritative final upper residual, and B4-247 replaced its decorrelated
physical-tail enclosure with a correlated clearance-seed formulation. That
formulation passes production-shaped native lifecycle tests but has not yet
reached the terminal match in a real banana4 run; B4-248 was deliberately
paused during atlas preparation. No final result has passed the independent
Bessel oracle.
