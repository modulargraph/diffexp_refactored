The survey is complete — all 8 package files read in full, every public/private symbol cross-referenced against `FeynmanTrick/`, `DiffExp/`, `Scripts/`, `Tests/`, and the battery/driver scripts. Here is the deliverable.

# FeynmanTrick Refactoring Plan

Paths abbreviated: `DEI` = `/Users/mhidding/Code/diffexp_refactored/FeynmanTrick/DiffExpIntegration.m`, `FTI` = `.../FeynmanTrickIteration.m`, `FIRE` = `.../FIREInterface.m`, `BC` = `.../BoundaryConditions.m`, `MX` = `.../MatrixExport.m`, `REG` = `.../DiffExp/RegularizedIntegration.m`. Battery = `Scripts/run_test_battery.sh` (17 suites). Quick example = `FT_EXAMPLES=bubble wolframscript -file Scripts/run_ft_stepwise.m` (compare `STEPWISE`/`FINAL` JSON against /tmp pySecDec references).

## P0 — Latent-bug fixes that also simplify (do first; they de-risk everything after)

1. **P0 | DEI:98,109,127,154,182,924 | Fix silent FEC key-context bug.** All six `DiffExp`State`FEC[RationalizationTolerance]` calls pass the *bare* symbol, which parses as `FeynmanTrick`DiffExpIntegration`Private`RationalizationTolerance` (DEI's context path is only `{FeynmanTrick`}`); FEC is a plain Association keyed by `DiffExp`State`RationalizationTolerance` (State.m:123), so every lookup returns `Missing["KeyAbsent",…]`. Lines 154/182 self-heal via the `10^-40` fallback; lines 98/109/127/924 silently disable real-chopping, theta-sign detection for negative endpoints, and `zeroQ`'s numeric branch. Exactly the NormalizeLogPower bug class. Action: one private accessor `rationalizationTolerance[] := With[{t = DiffExp`State`FEC[DiffExp`State`RationalizationTolerance]}, If[NumericQ[t] && t>0, t, 10^-10]]`, used at all six sites. Savings: ~8 lines + kills 6 copies of tol-fetch boilerplate. Risk: **medium** — it *activates* dormant tolerance logic (theta sign for pt<0, zeroQ numeric branch); watch `test_feynmantrick_integration_edge_cases.m` and the two regularized-integration suites. Gate: full battery + bubble.

2. **P0 | DEI:316-319, 576-577, 918-921, 1415-1418, 1500-1510 | One validated eps-prefactor accessor.** Five copies of "if prefactor list has wrong length, silently use `Table[0,…]`" — the silent J-vs-I basis misalignment trap. Action: `resolveEpsPrefactors[prefacs_, n_Integer, where_String]` that returns zeros only for `Automatic`/`{}` and Prints a loud warning (or `Throw`s) on a *nonempty wrong-length* list; ComputeLevelBoundary's matrix-pole inference fallback (1500-1510) stays as the documented fallback inside it. Savings: ~25 lines. Risk: low (current passing flows all have correct lengths). Gate: battery + bubble.

3. **P0 | DEI:1570-1573 (+1359-1362, +1450-1453) | Reduction-residual assertion in one shared extractor.** Three copies of `ibpCoeffs = Table[Coefficient[expr, Global`G[1, mastersAbove[[j]]]], {j,…}]`. Action: `ibpCoefficientsFor[expr_, masters_]` that also asserts `PossibleZeroQ[expr - ibpCoeffs.gVec]` (catches the whole master-list/index-misalignment class — leftover `G[1,…]` terms not in `mastersAbove` are currently dropped without a sound). Optionally reuse in FIRE:1082-1089. Savings: ~12 lines + the assertion. Risk: low. Gate: battery (esp. `test_feynmantrick_pipeline.m`) + bubble.

4. **P0 | DEI:757 | Replace hardcoded `expOrd = 30`.** Symbolic-transport-value branch in IntegrateCombinedMasters expands to fixed order 30 regardless of the run's ExpansionOrder. Action: derive from the sibling SeriesData in the same segment (`transportVal` branch above uses `transportVal[[5]]-transportVal[[4]]`), falling back to `DiffExp`State`FEC[DiffExp`State`ExpansionOrder]`; thread through as one local. Savings: 0 lines, removes a truncation cliff. Risk: low. Gate: battery + `test_integration_log_depth.m` specifically.

5. **P0 | DEI:269 | `"FixedParamValue" -> 11/23` default decoupled from config.** TransportLevel hardcodes 11/23 while `DefineFTIteration` (FTI:75) and `DeepestLevelBoundary` (BC:300) read `$FTConfig["FixedParameterValue"]`; neither RunIntegrationPipeline nor run_ft_stepwise passes the option, so changing the config option silently transports from the wrong basepoint. Action: default the option to `FeynmanTrick`Private`$FTConfig["FixedParameterValue"]`. Savings: 0; coherence fix. Risk: low (current value identical). Gate: battery.

## P1 — Dead code deletion (~335 lines, zero behavior change)

6. **P1 | FIRE:583-595, 598-631, 722-790, 937-1014, 1101-1110, 95-96 | Delete the batch/unused API surface.** `SetupFIREBatch`, `writeMultiProblemConfig`, `FindBasisBatch`, `ReduceIntegralsBatch`, `GetFIREResult`, `TopologyQ` have zero callers repo-wide (greps across FeynmanTrick/DiffExp/Scripts/Tests). Keep `ClearFIREState` (230-252) as a public utility even though its only caller dies with SetupFIREBatch — or delete it too for another 23 lines. Remove the matching `::usage` lines (30-52, 60-64). Savings: ~215-240 lines (FIRE drops ~20%). Risk: low. Gate: battery (`test_feynmantrick_fire.m`, `test_feynmantrick_iteration.m`) + bubble.

7. **P1 | FTI:484-546, 599-607, 31-33, 48-52 | Delete `IdentifyNeededIntegrals`, `GetLevelTopology`, `GetLevelMatrix`.** Zero callers; the needed-integral logic was superseded by `BoundaryRequestRecords`/`FeynmanTrickSeedMasters`. Savings: ~72 lines. Risk: low. Gate: battery.

8. **P1 | BC:225-242, 157-165 | Delete `EvaluateTadpoleNumerical` and `EvaluateRescaledParams`.** Zero callers. `RescaledFeynmanParameters` (108-128) is pipeline-dead but pinned by `Tests/test_feynmantrick_endtoend.m:56-75` as a paper-eq-2.11 reference — keep it (or delete together with that test block; owner's call). Savings: ~27 lines. Risk: low. Gate: battery (`test_feynmantrick_endtoend.m`).

9. **P1 | MX:117-135 | Delete `LoadGeneralMatrix`** (zero callers; `check_transport_ode_residual.m:41` reads `*_full.m` via raw `Get`). Note `ExportGeneralMatrix` is **not** dead — that script consumes its output and `test_feynmantrick_iteration.m:173` asserts the file exists. Savings: ~19 lines. Risk: low. Gate: battery.

10. **P1 | FTI:453-467 | Stop writing `eps_prefactors.m` — or make ComputeLevelBoundary read it.** `Put[prefactors, …/eps_prefactors.m]` is write-only repo-wide. Two defensible endpoints: (a) delete the Put (1 line) — prefactor info already flows in-memory via `DeepestLevelBoundary`/`ComputeLevelBoundary` returns; (b) keep it and make it the *authoritative* source replacing CLB's matrix-pole re-inference fallback (DEI:1500-1510). Recommend (a) now, since the in-memory path is the one debugged. Savings: 2 lines + one fewer stale artifact. Risk: low. Gate: battery + bubble.

11. **P1 | DEI:73, 509-510, 513 | Delete `$thetaRules` (never read) and the `"LowerResult"/"UpperResult"` result keys** (write-only repo-wide; they duplicate the full SegmentData in memory and triple the size of `FT_SAVE_TRANSPORT_DIR` dumps — `check_transport_ode_residual.m` only reads `SegmentData`/`NumIntegrals`). `"FixedParamValue"` key (513) also unread; keep it (cheap provenance). Savings: ~4 lines + large dump/memory savings. Risk: low-medium (interactive sessions may inspect them; verify owner). Gate: battery + bubble with `FT_SAVE_TRANSPORT_DIR` set, then `check_transport_ode_residual.m` on the saved file.

12. **P1 | Tests/test_transport_level.m | Bit-rotted: hardcoded `/private/var/folders/...T/FT_pipeline_test_32517` path** (line 18) from a dead process; not in the battery. Delete or regenerate from `test_feynmantrick_pipeline.m`'s output dir convention. Risk: none (not gated).

## P2 — Duplication consolidation

13. **P2 | FTI:210-239 vs DEI:1282-1324 | Unify the integrate/limitUpper/limitLower/direct classifier.** `FeynmanTrickNeededIntegral` and `BoundaryRequestRecords` re-implement the identical Which/Switch (the known lead at DEI:1291-1311 / FTI:216-236). Drift here would make seed-master closure and boundary requests disagree → silently wrong bases. Action: export `BoundaryRequestRecords` (and a single-vector `ClassifyBoundaryRequest`) from `FeynmanTrick`FeynmanTrickIteration`` with `::usage` (it loads before DEI per FeynmanTrick.m:15-21); DEI calls it fully qualified; FTI's `FeynmanTrickNeededIntegral` becomes `#NeededVec &`. Bonus: kills the `export_pysecdec_family_specs.m:171` Private reach-in (item 20). Savings: ~30 lines. Risk: low. Gate: battery + bubble.

14. **P2 | DEI:618-631 and 977-991 (and 374-381) | One `alignVariableContext[expr, targetVar]` helper.** Three SymbolName-matching remap blocks doing the same thing. Action: extract helper (≈6 lines), call at all three sites. Root-cause option (separate, riskier step): pin parameter creation to `Symbol["Global`xx" <> ToString[level]]` at FTI:136 so FIRE (Global`) and DiffExp (filename-derived Global`) always agree, demoting the remaps to assertions — do this only as its own batch since `stripContexts` (MX:23-31), `buildFIRESubstitution` (FIRE:215-226), and `extractVariables` (FIRE:204-211) all exist to paper over runtime-context symbol creation. Savings: ~20 lines now; ~40 more with the root-cause fix. Risk: low (helper) / **medium-high** (context pinning). Gate: battery + `test_symbols_namespacing.m` + bubble,sunrise.

15. **P2 | DEI:75-105 vs REG:~80-105 | De-duplicate `activeNumericPrecision`/`numericAtActivePrecision`/`realNumericAtActivePrecision`.** Verbatim copies; the DiffExp copy has the *correct* FEC key context, so deleting the FT copy and exporting the trio from `DiffExp`Utilities`` (or REG) simultaneously fixes item 1 at three of its sites. Savings: ~31 lines in FT. Risk: low-medium (cross-package; do export-with-`::usage` first, reload-test via `test_package_loading.m`). Gate: battery + bubble.

16. **P2 | DEI:1143-1256 vs REG:177-232 vs Scripts/run_ft_stepwise.m:47-53 | Shared Laurent-association module.** Three implementations of the `<|"MinPower"→…, "Coefficients"→…|>` algebra. FT's `LaurentCoeff/Zero/Add/Scale/Trim/MaxPower/ToNonNegativeList` duplicate REG's private ones with different chop semantics (FT: `Together@Expand` + `zeroCoeffQ` 10^-40; REG: `PChop` + `EffectiveZeroExprQ`). Action: export the core set from REG (or a new `DiffExp`Laurent``) with `::usage`; delete the FT copies and run_ft_stepwise's `laurentCoefficient`; keep FT-specific `LaurentToRange`, `MultiplyLaurentShifted`, `ExpandIBPCoeffLaurent`, `ShiftRawBoundariesToFinite` in DEI. Decide one chop semantic (recommend REG's PChop, which `test_regularized_integration*.m` already pins via Private reach-ins). Savings: ~80 lines net. Risk: **medium** (chop semantics differ; boundary values could shift at the 10^-40 level). Gate: full battery with special attention to `test_regularized_integration.m`, `test_regularized_integration_edge_cases.m`, `test_feynmantrick_pipeline.m` + bubble,sunrise vs /tmp references.

17. **P2 | DEI:240-254, 701-712, 938-941 vs REG:2075+ | Use DiffExp's `segmentMainExpression`/`uncompressSeriesData`.** DEI re-implements series-uncompress and "extract main-var expression from seg[[1]]" (twice). Export both from REG with `::usage`; DEI calls them; also fixes the `check_transport_ode_residual.m:36,46` Private reach-ins. Savings: ~25 lines. Risk: low. Gate: battery + bubble.

18. **P2 | DEI:1752-1884 vs Scripts/run_ft_stepwise.m:154-227 vs Tests/test_feynmantrick_pipeline.m:275-385 | The level loop exists three times — and `RunIntegrationPipeline` has zero callers.** The real, validated driver is run_ft_stepwise.m (which reaches into Private for `RequiredTransportEpsilonOrder` at line 156); the test hand-rolls a third copy that *omits* `CollectLevelIBPSingularFactors` and order trimming (already drifted). Action: extract exported `FTLevelStep[ftData, level, currentBCs, currentPrefactors, epsOrder, outputDir, opts]` performing trim→export→collect-factors→TransportLevel→ComputeLevelBoundary, returning `<|"BoundaryValues", "EpsPrefactors", "TransportResult", "TransportOrder"|>`; RunIntegrationPipeline's Phase-3 body and run_ft_stepwise's Do-body become thin wrappers (stepwise keeps its JSON printing and FT_SAVE_TRANSPORT_DIR hooks around the call). Leave the pipeline test manual (it asserts intermediate states) or migrate it last. Savings: ~120 lines across files; eliminates the worst drift hazard in the repo. Risk: **medium**. Gate: full battery + bubble,sunrise stepwise diff against /tmp pySecDec references (`Scripts/compare_stepwise_log.py`).

## P3 — Oversized-function splits (readability; net-zero lines)

19. **P3 | DEI:267-525 `TransportLevel` (259 lines) | Split into 4.** (a) `configureDiffExpForLevel[matrixDir, opts]` = 321-367; (b) `resolveSegmentationFactors[extraFactors, detectedVar, verbosity]` = 371-408 (remap + appendMatrixFactors + snapValues + deltaPrescriptions); (c) `transportOneDirection[diffExpConfig, deltaPrescriptions, remappedFactors, startPoint, bcs, endpoint]` — absorbs the duplicated reload-config+re-add-prescriptions block (459-469) so both directions share one path (425-486); (d) `combineTransportHalves` = 488-516. Risk: low-medium. Gate: battery (`test_feynmantrick_pipeline.m`, `test_feynmantrick_endtoend.m`) + bubble.

20. **P3 | DEI:562-892 `IntegrateCombinedMasters` (331 lines) | Split into 6.** (a) `prepareIBPCoeffOrders` (580-631: Laurent-expand + context remap); (b) `trimIncompleteTopOrders` (642-667); (c) `combineSegmentSeries[seg, …]` (681-794) with inner `masterContributionAtOrder` (716-784) — also hoist the `uncompressSeriesData[segData[[1,5]]]` double-uncompress (643 vs 690); (d) `debugPrintCombinedSegments` (807-842, the Verbosity≥3 block); (e) `allLaurentZeroQ` (847-864); (f) the integrate-and-scale tail (866-892). The function body becomes a ~50-line orchestrator. Risk: low-medium. Gate: battery (`test_feynmantrick_integration_edge_cases.m` calls it directly with a synthetic transport — best unit gate) + bubble.

21. **P3 | DEI:908-1115 `EvaluateLimitFromTransport` (208 lines) | Split into 3.** (a) `selectEndpointSegment[segData, boundary]` (934-967); (b) `masterEndpointLimit[seriesAtMaster, targetSeg, localEndpoint, …]` (1007-1067, the sector-vs-interior branch); (c) `coeffsAtBoundary` evaluation (1072-1098) — which shares its convolution tail (`MultiplyLaurentShifted` + `LaurentAdd`) with ComputeLevelBoundary's direct case (1619-1633): extract `convolveIBPWithValues[coeffLaurent, valuesLaurent, shift, maxPower]` used by both. Risk: low-medium. Gate: battery + bubble (bubble's pipeline exercises limitLower/limitUpper/direct).

22. **P3 | DEI:1473-1656 `ComputeLevelBoundary` (184 lines) | Split per case.** (a) `resolvePrefactorsAbove` (1492-1510 → item 2's accessor); (b) case handlers `integrateCaseBC`/`limitCaseBC`/`directCaseBC` (1580-1638) so the master Table is a dispatch; directCaseBC reuses `convolveIBPWithValues` from item 21. Risk: low. Gate: battery + bubble.

23. **P3 | DEI:1663-1905 `RunIntegrationPipeline` (243 lines) | Split into phases** using item 18's `FTLevelStep`: `phaseComputeMatrices` (1700-1709), `phaseDeepBoundary` (1711-1745), loop over `FTLevelStep` + checkpoint/stop-handling (1747-1884). Also: options `"CheckpointDirectory"`/`"StopAfterBoundaryLevel"` have zero callers — keep only if the checkpoint workflow is still wanted, else delete (~45 lines). Similarly TransportLevel options `"DeltaPrescriptionSign"`, `"PrescribeMatrixFactors"`, `"LowerEndpoint"`, `"UpperEndpoint"`, `"HomogeneousSolve"`, `"IntegrationStrategy"`, `"EstimateError"` are never overridden by any caller — likely interactive knobs from the box-seg12 campaign; recommend keeping the first four (campaign live per memory) and pruning only with owner sign-off. Risk: low. Gate: battery + bubble.

## P4 — Cross-package symbol hygiene inventory (complete list)

24. **P4 | Exports to add (script reach-ins into Private — the reload-no-op hazard):**
    - `FeynmanTrick`DiffExpIntegration`Private`RequiredTransportEpsilonOrder` ← `Scripts/run_ft_stepwise.m:156`. Add `::usage`, move above `Begin["`Private`"]`.
    - `FeynmanTrick`DiffExpIntegration`Private`BoundaryRequestRecords` ← `Scripts/export_pysecdec_family_specs.m:171` (solved by item 13).
    - `FeynmanTrick`Private`DimensionExpression[]` ← `Scripts/export_pysecdec_family_specs.m:104`. Export as `FTDimensionExpression` in `FeynmanTrick``.
    - `FeynmanTrick`DiffExpIntegration`Private`snapMainExpression` ← `Scripts/check_transport_ode_residual.m:37`.
    - DiffExp side (same scripts): `DiffExp`RegularizedIntegration`Private`{segmentMainExpression, uncompressSeriesData, segmentActualBounds, IntegrateSegmentWithPrefactorLaurent, laurentIntegralDump}` ← `check_transport_ode_residual.m:36,46`, `eval_dump_generic.m:14,35,38`. Export with `::usage` (item 16/17 covers two).
    - Verified clean: all `DiffExp`X`Y` calls *inside FeynmanTrick package code* target `::usage`-exported symbols (`PChop`, `TransportTo`, `DecomposeSingularity`, `DefiniteIntegralWithPrefactorLaurent`, `EvaluateEndpointLimitSectors`, `LoadConfiguration`, `UpdateConfiguration`, `FEC`, all `State` vars). The only cross-package defect is the FEC *key* context (item 1). No `FeynmanTrick`Private`$FTConfig` pokes from Scripts/Tests — all go through `SetFTOption` (good). Test reach-ins into DiffExp Private (`LaurentTrim`, `GetLeadingPower`, `SafeNumericLinearSolve`, etc.) are white-box by design; leave.
    Risk: low. Gate: battery (`test_package_loading.m`, `test_symbols_namespacing.m`) + run each touched script once.

25. **P4 | FeynmanTrick/IMPLEMENTATION_STATUS.md (Jan 25, pre-debugging-campaign) is stale** relative to `Docs/FeynmanTrickBananaStatus.md`; mark superseded or fold into Docs/. No test gate.

## Execution batches (each ends: full battery + one stepwise example vs /tmp references)

- **Batch A — pure deletions** (items 6,7,8,9,10,11,12): no behavior change by construction. Gate: battery + `FT_EXAMPLES=bubble` stepwise.
- **Batch B — hardening + hygiene exports** (items 1,2,3,4,5,24): the FEC fix and prefactor validation; smallest diffs, highest defect-prevention value. Gate: battery (watch both regularized-integration suites) + bubble.
- **Batch C — FT-internal de-dup** (items 13,14-helper-only,21's `convolveIBPWithValues`, 3's extractor if not done in B): Gate: battery + bubble,sunrise.
- **Batch D — cross-package consolidation** (items 15,16,17): DiffExp exports first, reload-test, then delete FT copies. Gate: battery + bubble,sunrise + `eval_dump_generic.m` replay on an existing /tmp dump.
- **Batch E — structural splits + pipeline unification** (items 18,19,20,21,22,23): biggest diff; do last on a green base. Gate: battery + bubble,sunrise stepwise diff via `Scripts/compare_stepwise_log.py` + `check_transport_ode_residual.m` on one saved transport.
- **Optional Batch F — root-cause context pinning** (item 14b, `Global`xx<k>` at FTI:136): only after A-E are green; medium-high risk, full battery + all examples.

Estimated total: ~600-650 lines removed from the 4,706-line package (~13%), three drift-prone triplications collapsed to one path each, and the two silent-failure classes (FEC key mismatch, wrong-length prefactor zero-fill) converted to loud failures.