# Tests/refs — vendored validation reference inventory

Vendored 2026-06-11 (M0 task 14, Docs/RewritePlan.md).  Master pin document:
`Tests/PINS.md`.  Everything here was copied verbatim from /tmp (the only
prior storage — power-loss lesson).  Sub-manifests:
`generators/README.md` (pin generators + venv rebuild + runtimes),
`oracle_logs/MANIFEST.md` (PASS vs FAIL stepwise records),
`oracle_dumps/README.md` (pre-existing: kernel-generated parity dumps).

## pysecdec/ — frozen reference values and driver inputs

Result files (FROZEN — do not regenerate in place; new runs go to new files):

| file | copied from | what it is |
|------|-------------|------------|
| `box1l_pin_result.log` | /tmp/box1l_pin_result.log | THE box L0 pin: 1-loop box loop_package result with full Cuhre error estimates (epsrel 1e-7, maxeval 5e6), JSON line prefixed `PIN`.  Source of the {12, -0.334914246809752, -41.28416739757452, -70.1654426808142} pin. |
| `loop_package_box_results.jsonl` | extracted verbatim from /tmp/pysecdec_refs_run.log (the two `PYSECDEC_JSON` lines, prefix stripped) | box_triangle and box_bubble loop_package results with full precision + error estimates.  Source of the box_triangle {2.25, 2.346284806971, 4.924311554762, 11.944387049203, 9.470615214143} and box_bubble {-0.5, -0.422784335096, -0.356269003210, +4.877038708178} pins. |
| `pentagon_pin_result.txt` | tail -6 of /tmp/pysecdec_pentagon.log (verbatim excerpt; full 155 KB log is build noise) | pentagon loop_package result (Cuhre epsrel 1e-8, maxeval 3e6): {-0.309523809524, +0.0164223473, +0.0254117770, -0.2797210931} + errors.  PROVISIONAL pin (R9). |
| `results_box1l.json` | /tmp/pysecdec-ft-box1l/results.json | family-route (exact FT normalization) box masters at t = 11/23, levels 0-3, written by Scripts/pysecdec_family_driver.py.  Loadable via `compare_stepwise_log.py --refs`. |
| `results_box_t1_20.json` | /tmp/pysecdec-ft-box-t1_20/results.json | box L1 needed-integrand {2,0,1,1} at t = 1/20 (left of the interior 1/4 pole). |
| `results_box_t2_5.json` | /tmp/pysecdec-ft-box-t2_5/results.json | same at t = 2/5. |
| `results_box_t19_20.json` | /tmp/pysecdec-ft-box-t19_20/results.json | same at t = 19/20. |

Trust notes for `results_box1l.json`:
- box_L0_1x1x1x1: NOISY GARBAGE (-175.59 +/- 10.7 / 3494 +/- 1994) — the
  3-variable geometric_no_primary package never converged.  Do NOT use as a
  pin; the box L0 pin is `box1l_pin_result.log`.  Kept for spec provenance.
- box_L1_1x0x1x0 and box_L1_1x0x0x1: eps^-1 = 1 good; eps^0 = nan (Cuhre
  failure).  The exact eps^0 values come from the closed forms /
  sympy cross-check instead (PINS.md section 3, item 2).
- All other entries (L2/L3 + `_needed` rows) are clean.
- COMPARATOR CAVEAT: `compare_stepwise_log.py --refs` SKIPS entries whose
  source_name ends `_needed` (Scripts/compare_stepwise_log.py:65-66).  The
  three t-variant files contain ONLY a `_needed` entry, so they load as
  empty ref sets — read their `summary` fields directly.

Driver input specs (regeneration inputs, produced by
`Scripts/export_pysecdec_family_specs.m`):

| file | copied from | contents |
|------|-------------|----------|
| `specs_banana.json` | /tmp/pysecdec_banana_specs.json | 11 banana specs at z0 = 11/23 (L0, 6x L1, 3x L2, L3) — regenerates the hardcoded banana comparator refs. |
| `specs_box.json` | /tmp/pysecdec_box_specs.json | box family specs at t = 11/23 (input that produced results_box1l.json). |
| `specs_box_t1_20.json` / `specs_box_t2_5.json` / `specs_box_t19_20.json` | /tmp/pysecdec_box_specs_t*.json | box specs at the three alternate t points. |

## generators/ — vendored pin-generator scripts

See `generators/README.md`.  Contents: the four /tmp/pysecdec_*.py scripts
(smoke, box1l_pin, ft_boxes, pentagon) plus `crosschecks/` (five
sympy/mpmath independent evaluators used to verify box boundary rows and
the FIRE reduction identity without pySecDec).

## oracle_logs/ — old-core STEPWISE campaign records

See `oracle_logs/MANIFEST.md`.  PASS records: l2_box.log, l2_banana.log,
l2_bubsun.log.  FAIL baselines: f_pentagon.log, f_box_triangle.log,
ft_box_bubble_run2_budget_starved.log.  Plus the three zsh run scripts that
produced them (exact env settings).

## oracle_dumps/ — kernel-generated parity artifacts (pre-existing)

Generated by the parallel M0 kernel-oracle task (see oracle_dumps/README.md):
dispatch_bubsun.tar.gz (809 solver-input dumps), laurent_bubsun.tar.gz,
m0_oracle_bubsun.log, m0_oracle_boxbubble.log (the box_bubble PASS record).

## NOT FOUND (regeneration queue)

Full list with regeneration instructions: `Tests/PINS.md` section 6.
Summary: the four original family-route results.json sets
(bubble/sunrise/banana-deep/banana-level1-pos-factored), the banana L0
direct-integration generator, the banana z0=2/5 second-point results.json,
the box L2 apparent-chart J1/J2 20-digit values, the banana L1 known-good
Laurent dumps, the old-core box eps^1 record, and a complete l2_boxbubble
log (superseded by oracle_dumps/m0_oracle_boxbubble.log).
