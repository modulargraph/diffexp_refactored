Survey complete. I read every file in `DiffExp/` and `DiffExp/IntegrationStrategies/`, grepped all usages across `DiffExp/`, `FeynmanTrick/`, `Scripts/`, `Tests/`, audited cross-package context references, and mapped the gating tests (`Scripts/run_test_battery.sh` = the 17-suite battery; heavier physics suites run separately). No wolframscript was executed.

# DiffExp Refactoring Plan (static survey, 2026-06-11)

**Cross-package no-op audit result (bug class from NormalizeLogPower):** the only unexported symbols referenced cross-package are `DiffExp`State`` debug flags — `$LogStrategyDispatch`, `StrategyDispatchLog` (Dispatch.m:63-68), `$DebugFuchsianizedCheck`, `$DebugBlockResidualSeries` (ResonantRecurrence.m:956,1226,1339,1380,1438,1502,1522; Scripts/run_ft_stepwise.m), and `LastBoundaryMatchingContext` (Transport.m:394,419). All are *assigned and read* with full qualification, so they work today, but they are exactly one un-qualified rename away from the silent no-op class. No live instances of the bug were found. `Reference/DiffExp_original.m` is read by nothing in the repo (safe; leave as documentation).

## Prioritized item list

| # | Pri | File:line | Action | Saves | Risk | Gating test |
|---|-----|-----------|--------|-------|------|-------------|
| D1 | P1 | SeriesOps.m:14,56,77-78 | Delete `SeriesCoefficientMinus` (defs, ::usage, entry in SetAttributes). Zero callers repo-wide. | 5 | low | test_package_loading, test_symbols_namespacing |
| D2 | P1 | SeriesOps.m:12,56,71 | Delete `SSN` (alias of SN, zero callers) incl. SetAttributes entry. | 3 | low | same |
| D3 | P1 | SeriesOps.m:28,153-159 | Delete `MatrixPowerSExpand` (zero callers). | 9 | low | same |
| D4 | P1 | RegularizedIntegration.m:439-444 | Delete 4-arg `ApplyRegularizationStep` overload — zero callers (tests use 5-arg), and its body is an identity repack (`{r[[1]],r[[2]],r[[3]],r[[4]]}` of a 4-list). Also fix stale ::usage at line 45 (says returns 3 elements; 5-arg returns 4). | 7 | low | test_regularized_integration |
| D5 | P1 | RegularizedIntegration.m:1916-1918,1873 | Delete write-only `atLowerSing`/`atUpperSing` in `IntegrateSegmentData` (assigned, never read) + their declarations + comment. | 4 | low | test_regularized_integration |
| D6 | P1 | Transport.m:1249-1252 | Delete redefinitions of `IntervalOverlapQ`/`IntervalIntersec`/`IntervalContainsQ` — they re-define the *same symbols* already imported from Utilities.m:112-114 with identical bodies. | 4 | low | test_transport_level |
| D7 | P1 | LineSegmentation.m:66; Transport.m:515; AnalyticContinuation.m:19 | Drop never-used Module locals: `RatPoles, Sqrts, NumPoles, Tmp2, Tmp3, Tmp4` (FindMatrixSingularities); `CurrIntegratedError, tempFile` (TransportTo); `AllPrescriptions` (PrepareAnalyticContinuation). | 2 | low | test_package_loading |
| D8 | P1 | SeriesOps.m:56 | Remove duplicate `SN` and orphan `SEval` from the SetAttributes list (`SEval` there creates a dead `DiffExp`SeriesOps`Private`SEval`; the real `DiffExp`Pade`SEval` never gets that attribute and doesn't need it — its catch-all delegates to Listable `SEval2`). | 1 | low | test_package_loading |
| D9 | P1 | Default.m:124 | `ReportError[...][1]` — ReportError takes no level argument; drop the `[1]`. Also RegularizedIntegration.m:63 `IntegrateSegmentData::usage` says 2 args, definition takes 4 — fix text. | 0 | low | test_package_loading |
| D10 | P2 | Transport.m:1269 | Broken validation: `MatchQ[SavedData[[0]] === List]` is operator-form MatchQ, so the whole `If` condition is non-boolean and the error check never fires. Replace with `!(ListQ[SavedData] && Quiet[Dimensions[SavedData][[2]] === 5] === True)`. Behavior change: the check starts working — verify test_topiecewise still passes. | 0 | low | test_topiecewise |
| N1 | P2 | State.m (~line 79) | Add ::usage lines for `$LogStrategyDispatch`, `StrategyDispatchLog`, `$DebugFuchsianizedCheck`, `$DebugBlockResidualSeries`, `LastBoundaryMatchingContext` (hardens the silent-no-op bug class; documents debug API). | -6 | low | test_package_loading |
| N2 | P2 | Utilities.m + Recurrence.m:643-648,673-685,758-768; ResonantRecurrence.m:1130-1136; Dispatch.m:10-16 | Add one exported helper `DebugEnvPrint[envvar_String, args__]` (and `DebugEnvDump[envvar, expr]` for the Put/dump variant) in Utilities.m; collapse the five env-gated debug blocks to single calls. Must keep env var names `DEBUG_SING_PART`, `DEBUG_POWER_COEFF`, `DEBUG_DUMP_DISPATCH_DIR` intact (Scripts rely on them). | 30 | low | test_singular_recurrence, test_recurrence_no_fallback |
| C1 | P2 | RegularizedIntegration.m:1944-1953, 2011-2020, 2219-2225, 2280-2286 | Factor the 4 identical "unwrap savedData2 Association/SegmentData" blocks into private `resolveSegmentData[arg_, fname_String]`. | 25 | low | test_regularized_integration, test_regularized_integration_edge_cases |
| C2 | P2 | RegularizedIntegration.m: define `resolveBranch[e_, rules_] := PChop[Expand[branchReplaceAtActivePrecision[e, rules]]]` | Replace ~9 verbatim occurrences (550-557, 857, 1096-1098, 1368-1370, 1719-1721, 1813-1815, 1336-1339, …). | 12 | low | test_regularized_integration_edge_cases |
| C3 | P2 | RegularizedIntegration.m:1306-1311, 1340-1347, 1791-1796, 1816-1821 | Factor `positiveLogPowers[expr_, tol_]` (Select of LogxPowerRange with nonzero LogxCoeffNS, powers > 0). | 12 | low | test_regularized_integration_edge_cases |
| C4 | P2 | Transport.m:1293-1304 (ToPiecewise) | Compute the shared rule chain (`Logx->Log[x]`, θp/θm→HeavisideTheta, `SavedData[[ind,2]]`) once; branch only on `Pade===True` head (`ProjectΘs[#,GetPade]` vs `Normal`). | 8 | low | test_topiecewise |
| C5 | P2 | Transport.m:186-192 vs 531-537 | Factor identical bcs2-unwrap into private `normalizeBoundaryInput[bcs2_]`. Also factor the dup `DeleteCases[...//System`Variables, "?"|x|Logx]` at 565 and 968 into `freeIndeterminates[expr_]`. | 10 | low | test_transport_level |
| C6 | P2 | Dispatch.m:73-82 vs 87-131 | Compute `strategy` (string) once with a single Which; use it both for the `StrategyDispatchLog` entry and a string-keyed dispatch. Removes the duplicated condition logic. | 15 | low-med | test_recurrence_no_fallback, test_singular_recurrence |
| C7 | P3 | RegularizedIntegration.m:2207-2267 vs 2269-2345 | Make `DefiniteIntegralWithPrefactor` a wrapper: call `DefiniteIntegralWithPrefactorLaurent[..., 0]` and map `LaurentToNonNegativeList[#, numEpsOrders-1]&` (same pattern as IntegrateSingularTerm:482-487 already uses). Note: only the Laurent twin calls `maybeDumpLaurentDefiniteIntegral` — wrapper inherits it; acceptable (env-gated). | 55 | med | test_regularized_integration_edge_cases, test_feynmantrick_integration_edge_cases |
| C8 | P3 | RegularizedIntegration.m:2352-2558 vs 2560-2730 | `IntegrateSegmentWithPrefactor` (non-Laurent) is a near-verbatim twin of the Laurent one (the modifiedDecomp construction 2460-2547 ≡ 2634-2709 verbatim). Replace the whole non-Laurent body with: `LaurentCoeff[IntegrateSegmentWithPrefactorLaurent[seg,{a,b},intIndex,0,alpha,beta,rationalFactor,variable,lower,upper], epsOrder]`. Only delta: LaurentScale/LaurentAdd apply PChop (chop at 10^-250) that the non-Laurent path lacks — numerically inert. Also delete write-only `mainMin/mainMax/localMin/localMax` assignments (2374-2377, 2580-2583) and dead declarations `prefactorSeries, combinedDecomposition, localSeries, slope` in both. | 200 | med-high | test_regularized_integration_edge_cases, test_interior_singular_integration, test_feynmantrick_pipeline + endtoend |
| C9 | P3 | RegularizedIntegration.m:542-549, 724-732, 1087-1095 | Factor identical branchDir/branchPoint/branchRules computation into `endpointBranchData[{xmin_,xmax_}, atLower_, atUpper_]` returning `{branchDir, branchPoint, branchRules}`. | 18 | med | test_regularized_integration_edge_cases, test_singular_endpoint |
| C10 | P3 | RegularizedIntegration.m:369-372, 497-498, 722-723, 797-798, 1084-1085, 1435-1438 | Replace the six per-function `zeroQ` closures with module-level `NumericZeroQ[z, tol]` (already exists at 265-271; semantics match — Re-projection does not change `Abs[..]<tol`). | 18 | med | test_regularized_integration, test_interior_singular_integration |
| C11 | P3 | SeriesOps.m:43-52 / RegularizedIntegration.m:82-94 / SingularityDecomposition.m:37-46 | Three identical copies of `activeNumericPrecision`/`numericAtActivePrecision`. Export both from `DiffExp`Utilities`` (::usage BEFORE `Begin["`Private`"]` — this is the no-op bug class, add to test_symbols_namespacing) and delete the copies. Leave FeynmanTrick/DiffExpIntegration.m:75 copy alone (separate package). | 22 | med | test_package_loading, test_symbols_namespacing, test_regularized_integration |
| C12 | P3 | RegularizedIntegration.m:273-284 vs SeriesOps.m:95-106 | `IntegerPower` ≡ `NormalizeLogPower` (only delta: real-projection before rounding). Keep one exported implementation (suggest `DiffExp`SeriesOps`NormalizeLogPower` with the realNumeric guard merged in) and define `IntegerPower = DiffExp`SeriesOps`NormalizeLogPower` locally. `NormalizeXPower` (SingularityDecomposition.m:52-64) is different (rationalizes non-integers) — leave it. | 10 | med | test_integration_log_depth, test_decomposition |
| C13 | P4 | Recurrence.m:111-121,124-127; ResonantRecurrence.m:23-39, 51-60, 660-663, 1313-1315 | Reuse `ExpandedMatrixMinOrder` in `PrepareSingularRecurrence` and `GeneralSingularRecurrenceApplicableQ`; add `ResidueMatrix[ctx_] := ExtractAMatCoefficients[ctx, -1, -1][[1]]` and use at the four extraction sites. | 25 | med | test_singular_recurrence, test_resonant_2f1, test_recurrence_no_fallback |
| C14 | P4 | ResonantRecurrence.m:837-848 | This output block is exactly `BuildParticularFromCoeffs[fCoeffs, lambda, maxK, systemSize, maxOrd, wp]` (1026-1043) — call it. Similarly Recurrence.m:534-540 and 751-756 share the "P·gCoeffs → SeriesData·x^pow" pattern → tiny helper `eigenBasisSeries[P, gCoeffs, pow, maxOrd, systemSize]`. | 18 | med | test_resonant_banana, test_singular_recurrence |
| D11 | P4 | RegularizedIntegration.m:791-827 (+ Tests/test_regularized_integration_edge_cases.m:36 block) | `IntegrateAnalyticRegularizedByIBPLaurent` has zero production callers (superseded by the subtraction path at line 671); only one test calls it via `Private``. Delete function + that test section, or keep deliberately as reference — owner choice. | 37 | med (test edit) | test_regularized_integration_edge_cases |
| S1 | P4 | VOP.m:105-142 and 233-270; Helpers.m:62-107 | `ReduceIndeterminates` has ZERO callers; the same loop is inlined twice in VOP.m, where `IndeterminatesRemaining, currentConstants, seriesOrderOffset, VanishingTermsCurr, OverdeterminedEqns, Cmat, Cb, CsPartSol, CsNullVectors, CsGeneralSol, constantReplacements` are NOT in SolveVOP's Module list (they leak as Private-context globals). Replace both inline blocks with `ReduceIndeterminates[fGeneral, VanishingTerms, Length[fGeneral]]` (only delta: generic error text). Also factor the repeated MtildeMatrix selection (VOP.m:35-54 vs 197-205). | 80 | med — VOP has no direct battery coverage; needs an explicit `IntegrationStrategy->"VOP"` smoke run (e.g. bubble) | test_transport_level + manual VOP run |
| S2 | P5 | Transport.m:845-868 vs 1061-1081 | Factor segment-export into `appendSegmentExpansion[...]` (the two sites differ only in indentation); inside it, the ApplyAnalyticContinuation//ProjectΘs//SaveExpansionsOrder//Compress chain appears 2× per site → one local `exportableExpansion[CurrIntegrated]`. | 30 | med | test_topiecewise, test_regularized_integration (both consume SaveExpansions output) |
| S3 | P5 | Transport.m:666-750 vs 1112-1169 | The predivision segment-counting loop is an acknowledged copy ("Duplicate of the code block below…") of the main loop's segment-advance logic. Extract `nextSegmentTarget[PoleIntervals, currentCenter, CurrIntervalLine, to, FixAt, singularities, imaginarySingularities, line, CurrLine]` returning `{CurrEvalPoint, CurrEvalPointCurrLine, NextIsPole, newCenter, newCurrLine, newCurrLineNoMobius, done}`; counting loop uses it directly, main loop calls `EvaluateCurrPoint[]` between target computation and state update. | 60 | med-high | full battery + test_transport_level + test_banana_refactored |
| S4 | P5 | Transport.m:514-1247 (TransportTo, ~734 lines) | Split into private top-level functions: (a) lines 530-591 → `prepareTransportInputs[bcs2, line, to2]` (unwrap, precision raise, point→line conversion, error seeds); (b) 598-657 → `computePoleIntervals[line, FixAt, to]` (singularity segments + pole intervals incl. the Mobius variant); (c) 666-750 → `countPredivisionSegments[...]` (uses S3 helper); keep closures UpdateMatrixExpansionError/EvaluateCurrPoint/ComputeErrorsPerIndeterminate/PrintError; (d) 1234-1246 result assembly stays. Main body shrinks to ~300 lines. | 0 (structure) | med-high | full battery + test_unequal_mass_full |
| S5 | P5 | Transport.m:146-511 (IntegrateSystem ~365 lines) | Extract boundary-constant fixing (288-441) into `fixBoundaryConstants[fGeneral, cIndices, bcs, BCSRelevant, intind, epsord, FixAt]` (~150 lines, includes LinearSolve→checked-LeastSquares fallback). Add `benchPut[line, keys__]`/`benchTime[line, keys__]` helpers to collapse the 10+ `BenchmarkData["Segments"][line // N][...]` lines (161-164, 218-225, 233-237, 241, 253-256, 461, 466, 501). | 30 | med | full battery |
| C15 | P5 | Pade.m:80-88 | `SEval[a_SeriesData, at_]` is literally `SEval2[SEval1[a], at]` — replace body. Kills one of the four θ-rule literals in Pade.m. | 8 | low-med | test_transport_level, test_topiecewise |
| C16 | P6 (sign-off) | Transport.m:762-772 (UpdateMatrixExpansionError) | Lines 768/770 re-implement `SEval2` with `N[..]` instead of `SetPrecision`; could become `FiniteAbsMax[SEval2[CurrErrorTerms, pt]]`. Slight numeric difference (N vs SetPrecision) in *error estimation only* — propose, but flag. | 6 | med | test_transport_level + battery |
| C17 | P6 (sign-off) | RegularizedIntegration.m:286-308 vs SingularityDecomposition.m:257-281; RegularizedIntegration.m:119-174 vs SingularityDecomposition.m:138-220 | `EffectiveZeroExprQ` ≈ `EffectivelyZeroExpr` (delta: SetPrecision before θ-substitution) and `linearSeriesCombine` ≈ `AddCompatibleSeries` (deltas: chop flag; constant maxPow `1` vs `den`). Unification is possible but both sit at the center of the just-finished box/banana debugging — defer until seg12 work closes; if done, export the survivor from SingularityDecomposition with ::usage. | 75 | HIGH | test_decomposition, test_regularized_integration_edge_cases, test_interior_singular_integration + pySecDec replay |
| C18 | P6 (sign-off) | ResonantRecurrence.m:701-728 vs 761-824 | The n=0 and n≥1 "resolve previous kernel freedom via left-null overlap" blocks are ~85% identical → `resolveChainFreedom[Ln, k, fCoeffs, prevFreeParams, rhsBuilder]`. This code was fixed during the campaign (banana ε⁻¹ impurity) — only with full physics validation. | 45 | HIGH | test_resonant_banana, test_unequal_mass_banana_parity_speed + pySecDec replay |
| D12 | P6 (owner decision) | LocalSeries.m:~255-1026 | Entire finite-width solver block (`LaurentCoefficientsRational`, `Scalar/Matrix/RationalMatrixZEpsLaurentAssoc`, `AllZCoefficients*`, `ClearZDenominators`, `PrepareFiniteWidthData`, `RecursiveFiniteWidthSolve`, `SolveLocalFuchsianSeries`, `FiniteWidthCoefficient/Evaluate`, `ApplyGaugeToSolution`, `ValidateFiniteWidthSolution`) has ZERO production callers — production uses only `FuchsianizeLocal` + `TrimFuchsianLattice` (ResonantRecurrence.m:156,167). But it is the implementation of the AGENTS.md "finite-width" mandate and is covered by test_local_series_solver.m / test_local_series_edge_cases.m. DO NOT delete without owner sign-off; if approved: −~750 lines + retire test_local_series_solver.m. | 750 | owner | test_local_series_edge_cases (battery member) |

## Suggested execution order (batches; each batch = one battery run)

- **Batch 1 — pure dead code (zero behavior surface):** D1-D9, N1. ~35 lines saved. Validate: `Scripts/run_test_battery.sh`. Any failure here means a mistaken usage scan, not numerics.
- **Batch 2 — low-risk helpers, no numeric paths changed:** N2, C1-C6, D10, C15. ~120 lines. Validate: battery + test_topiecewise + test_transport_level (the latter two are NOT in the battery — run explicitly).
- **Batch 3 — RegularizedIntegration twins:** C7, then C8 (do C7 first; C8 deletes the big duplicate), then C9-C12. ~320 lines. Validate: battery (it contains all regularized/FT suites) + owner spot-check of a banana/box value via the /tmp pySecDec dump-replay workflow.
- **Batch 4 — recurrence family:** C13, C14, D11, S1. ~160 lines. Validate: battery + test_resonant_banana + test_unequal_mass_banana_parity_speed + one manual `IntegrationStrategy->"VOP"` run (no automated VOP coverage exists).
- **Batch 5 — Transport structural split:** S2, S3, S4, S5. ~120 lines + big readability win. Validate: battery + test_transport_level + test_banana_refactored + test_unequal_mass_full (slow suites).
- **Batch 6 — only with owner sign-off:** C16, C17, C18, D12. These touch validated numerics from the just-completed campaign (or delete test-mandated modules); each needs the full battery plus a pySecDec reference replay before merging.

Estimated total: ~750 lines removed in batches 1-5 (~7% of the 10.9k-line package) with no behavior change; up to ~1,600 if batch 6 is approved. The two real defects found en route: Transport.m:1269 (dead ToPiecewise argument validation, D10) and the unscoped globals in SolveVOP (S1).